---
id: TEL-001
title: Telemetry events carry an envelope, a bucket and an injected timestamp
slug: telemetry-events-carry-an-envelope-a-buc
epic: EPIC-02
type: feature
status: in-progress
phase: RED
branch: story/TEL-001-telemetry-events-carry-an-envelope-a-buc
depends_on: [ROUND-001]
required_gates: []
---

## Context

Brief **B6** says build telemetry in M2, not later, and **A5** says instrument the
three discovery buckets — D1, days 2–7, days 8–28 — **separately from day one**,
because Roblox's Recommended For You measures them separately and first-play
bounce is a direct ranking penalty.

This story builds the envelope and the sink interface. It emits nothing; `TEL-002`
and `TEL-003` add the emitters. Splitting it that way is deliberate: the envelope
is the thing every later emitter agrees on, and getting the bucket mapping wrong
is cheap now and expensive once nine call sites exist.

The architectural decision behind it (`architecture.md` §6, D3): **emission is an
effect, not a call.** Pure code returns `{ kind = "Emit", event }` and a driver
hands it to the sink. Three things follow: telemetry is verified with no sink at
all, a sink failure cannot break a round, and the events become part of the phase
machine's tested output rather than a side channel nobody asserts on.

**Which required gate would fail if this story's artifact broke:** `unit`.

## Acceptance criteria

- **AC-1** — Given a clock at `t`, when an event is constructed, then its `at`
  field is `t` — taken from the injected clock — and its `name`, `bucket`,
  `sessionId` and `fields` are the values supplied.
- **AC-2** — Given the bucket mapping, when it is read, then it names **all nine**
  B6 events with these buckets: `join` D1, `first_round_complete` D1,
  `bounce_before_resolution` D1, `session_end` D1, `round_complete` D2_7,
  `rematch_accepted` D2_7, `day_n_return` D8_28, `co_play_with_known_player`
  D8_28, `purchase` D8_28.
- **AC-3** — Given an event name not in the mapping, when an event is constructed
  with it, then construction fails rather than producing an event with a guessed or
  absent bucket.
  *Control:* an implementation that defaults an unknown name to D1 **must** fail
  this. A silent default is how a metric quietly starts measuring the wrong bucket.
- **AC-4** — Given a recording sink, when three events are emitted, then it holds
  exactly those three, in order, with their fields intact.
- **AC-5** — Given a sink whose `emit` raises, when an event is emitted through the
  dispatch helper, then the raise does not propagate to the caller, and the failure
  is itself recorded as a counted drop.
  *Control:* an implementation that lets the error propagate **must** fail this;
  one that swallows it with no count must fail the "counted" half. Telemetry that
  can crash a round is worse than no telemetry, and telemetry that silently drops
  is worse than either.
- **AC-6** — Given the `fields` table of an event, when a value is not a string,
  number or boolean, then construction fails naming the offending key.
  *Semantics:* `fields` is a flat map of scalars. A nested table is a payload
  nobody downstream can index, and a `PlayerId` that is an Instance is a memory
  leak and a replication hazard.

## Contract

### `src/shared/telemetry/Event.luau`

    export type Bucket = "D1" | "D2_7" | "D8_28"

    export type TelemetryEvent = {
        name:      string,
        at:        number,                       -- seconds, from the injected Clock
        bucket:    Bucket,
        sessionId: string,
        playerId:  string?,
        fields:    { [string]: string | number | boolean },
    }

    Event.BUCKETS : { [string]: Bucket }         -- frozen; all nine B6 names
    Event.new(name: string, at: number, sessionId: string,
              playerId: string?, fields: { ... }?) -> TelemetryEvent

`at` is passed in, never read. Nothing in this module calls a clock —
`ROUND-001`'s guard test would catch it, and that is the point of the guard.

### `src/shared/telemetry/Sink.luau`

    export type Sink = { emit: (TelemetryEvent) -> () }

    Sink.noop() -> Sink
    Sink.recording() -> RecordingSink            -- RecordingSink.events : {TelemetryEvent}
                                                 -- RecordingSink:clear()
    Sink.dispatch(sink: Sink, events: { TelemetryEvent }) -> DispatchResult
    -- DispatchResult = { delivered: number, dropped: number }

`Sink.dispatch` is where AC-5 lives: it isolates each `emit`, counts drops, and
never raises. It is the only function a driver calls.

### The three events declared and not emitted

`day_n_return`, `co_play_with_known_player` and `purchase` need persistence and are
M5. They are **in `BUCKETS` and have no emitter**, so M5 adds an emitter rather
than a vocabulary, and the gap is visible in the mapping rather than discovered
later. A test asserting "every name in `BUCKETS` has an emitter" would therefore be
wrong today; do not write one.

### `bounce_before_resolution` is the one to get right

A5 makes first-play bounce a direct ranking penalty, so it is the most valuable
event in the list and the easiest to emit twice or not at all. Its **rule** —
emitted when a player leaves while the phase is `Lobby`, `Assignment` or `Round`,
at most once per player per round, never during `Resolution` or `Post` — belongs to
`TEL-002`. This story only fixes its name and bucket.

### Test-only dependencies

None.

### Oracle partition

| AC | Kind | Instruction to RED |
|---|---|---|
| AC-1, AC-4, AC-6 | **Mechanical** | Pin exactly. |
| AC-2 | **Settled** | The nine names and their buckets come from brief B6 and A5. Read them out; do not invent an event and do not omit one because it has no emitter yet. |
| AC-3, AC-5 | **Mechanical, with controls** | Both controls are in the criteria. AC-5 has two halves and an implementation can pass one while failing the other — assert both. |

## Callers of changed signatures

**None. This story changes no existing signature.** Both modules are new files:
`src/shared/telemetry/Event.luau` and `src/shared/telemetry/Sink.luau`. Nothing in
the tree imports either one yet, and nothing in the tree exports a `TelemetryEvent`,
a `Bucket` or a `Sink` today.

Checked against the tree at PLANNED, on the story branch:

```
$ git ls-files src tests lune | xargs grep -ln 'telemetry\|TelemetryEvent\|Bucket\|Sink' 
(no output)

$ ls src/shared/telemetry 2>&1
ls: cannot access 'src/shared/telemetry': No such file or directory
```

So the hazard this section exists for — an existing caller that still compiles
through RED because the old signature is still there, then silently stops
collecting when GREEN deletes it — cannot arise. RED's handoff should state that it
re-ran this check against the tree rather than copying this claim forward.

## Orchestrator checks at PLANNED

**Gate coverage.** Read against this story's artifacts,
`src/shared/telemetry/Event.luau` and `src/shared/telemetry/Sink.luau`, from the
`covers` lines in `.claude/harness/project.conf`:

- `unit` — **required**, `covers: src/shared/**`. This is the gate named in
  `## Context`, and it genuinely reads the artifact: both modules live under
  `src/shared/`.
- `lint`, `typecheck`, `build` — all **required**, all `covers: src/**`.
- `coverage`, `integration`, `mutation` — optional and **unconfigured** (empty
  commands in `project.conf`). Nothing about this story's artifact is reachable
  only through them.

Nothing here is exercised only by an optional gate, so `required_gates` stays
empty.

`unit`'s floor is **221** today. Raising it to the new real count is a
`project.conf` edit, which belongs to the Lead PO at GATES, once that count
exists. `## Notes` already asks for it; recorded here so it is not a surprise.

**Epic done-when.** `EPIC-02`'s telemetry clause is *"The six B6 events a server
round can observe without persistence are emitted with the right bucket, exactly
once per occurrence, and every rejected remote call emits one naming its reason."*
That clause belongs to `TEL-002` and `TEL-003`, which are the emitters. TEL-001
delivers the vocabulary those emitters agree on and emits nothing, which the story
says in `## Out of scope` and the epic says in its own story list. **There is no
gap between the last DONE story and this one for the orchestrator to close**, and
no numbered PO decision is needed on the epic's behalf.

**AC-2 is settled, and the settlement was verified rather than assumed.** The nine
names and their buckets were read out of `docs/wiki/architecture.md` §6 (the B6
table, lines 406–416) and compared against AC-2 field by field. They match exactly,
including the three M5 events declared with no emitter. RED reads them out; RED does
not design an event set.

**The contract may be amended in place by RED, with a reason.** If a block in
`## Contract` turns out to be unbuildable or ambiguous as written, RED edits that
block and says why in the same place. GREEN builds what the amended block says.
Acceptance criteria are a different thing and are frozen — those take an
`## Amendments` entry and a PO decision.
## Deferred verifications

<!-- Nothing is deferred. The template requires this section only when a
     verification provably cannot run in the phase that wants it, and says
     to omit it otherwise - so the word "None" in prose here reads to
     check-boundaries.sh as a deferred entry with no owner and no result,
     which is the same defect as a WAIVED with no reason.
     Planned text was: None.
-->

## Model guidance

Planned by `bash scripts/plan.sh write TEL-001` from `.claude/harness/models.conf`.
A PLAN, not a record: a session setting or an explicit override can beat both
this and the agent's own `model:` field, and nothing here can see which won.
The orchestrator still writes down the model each dispatch **resolved** to, by
name, below the table.

| Phase | Agent | Planned | Why |
|---|---|---|---|
| PLANNED | `lead-po` | `opus` | planning is the judgement phase: decomposition, the oracle partition, and what goes in the contract |
| RED | `test-developer` | `fable` | the measured case. With a partitioned contract to work from, the brief carries the judgement and the weaker model writes sharper negative controls than the stronger one did without it |
| GREEN | `feature-developer` | `opus` | the failure mode of a weaker model here is reaching green by weakening a test, which is the one thing this harness exists to prevent |
| GATES | `feature-developer` | `opus` | same risk as GREEN, and a gate failure is where "make it stop complaining" is most tempting |
| REVIEW | `lead-po` | `opus` | reading review feedback against the contract is judgement, and a wrong call here ships |
| SCAFFOLD | `lead-po` | `opus` | source, tests and config in one indivisible derivation, with no failing test in front of any of it |

**Resolved:**

<!-- One line per dispatch, as it happened: phase, agent, the model that
     actually ran, and — if a phase was planned for one model and ran on
     another — what that changed. A choice with no verdict is folklore. -->
## Out of scope

- Any emitter. `TEL-002` (round lifecycle) and `TEL-003` (remote rejections).
- Any real sink. What telemetry goes to on a live server — an analytics service, a
  DataStore, Roblox's own AnalyticsService — is an M5 decision with request-budget
  and throttling consequences. Here, `noop` and `recording` are the only
  implementations, and that is sufficient because emission is an effect.
- Batching, sampling, back-pressure or retry. All belong with a real sink.
- Any client-side telemetry. There is no client.
- Personally identifying anything. `playerId` is a Roblox user id as a string and
  nothing else is carried.

## Notes

**Mutation the orchestrator should run at acceptance:**

1. Make `Event.new` default an unknown name to `"D1"` instead of failing.
   Predicted: AC-3 goes red alone.
2. Make `Sink.dispatch` count a raising `emit` as `delivered`. Predicted: AC-5's
   counted-drop assertion goes red and the no-propagation assertion stays green —
   the single-assertion case.
3. Remove `purchase` from `BUCKETS`. Predicted: AC-2 goes red.

Run mutation 2 first.

**Raise the `unit` floor** to the new real count.

## Test plan

Five test files, all under `tests/`, following the pattern SEAT-002 settled:
checks in a `*Contract.luau` helper parameterised over the subject, defective
subjects in a `*Stubs.luau` factory, the real suite per module, and a controls
suite that needs nothing from `src/shared/telemetry/` and therefore RUNS in RED.

| File | Covers |
|---|---|
| `tests/helpers/TelemetryContract.luau` | AC-1, AC-2, AC-3, AC-4, AC-5 (two checks), AC-6, plus `dispatch`'s positive half, as functions over any `Event` / `Sink` module. The settled nine-name table lives here (`Contract.BUCKETS`), read out of `architecture.md` §6. |
| `tests/helpers/TelemetryStubs.luau` | a factory over one-field defect tables: a correct baseline Event and Sink, eight wrong Events, five wrong Sinks |
| `tests/shared/telemetry_event_test.luau` | 5 tests against the real `src/shared/telemetry/Event.luau` - export shape, AC-1, AC-2, AC-3, AC-6. **All red in RED.** |
| `tests/shared/telemetry_sink_test.luau` | 5 tests against the real `src/shared/telemetry/Sink.luau` - export shape, AC-4 (direct emit), AC-4 (via dispatch), AC-5 no-propagation, AC-5 counted-drop. **All red in RED.** |
| `tests/shared/telemetry_controls_test.luau` | 15 tests: two baselines that pass every check, and thirteen controls each asserted to FAIL the check it is built for AND PASS the rest. **All green in RED.** |

Per test, what it asserts and which AC it covers:

| Test (`telemetry_event_test.luau`) | Asserts | AC |
|---|---|---|
| `Contract: Event exports BUCKETS as a table and new as a plain field function` | the two exports exist with those types | Contract |
| `AC-1: an event built with the injected clock's t carries at = t and exactly the supplied name, bucket, sessionId, playerId and fields` | over 5 cases (t = 0, 1234.5, 2^31, 7, 300 via `Clock.manual(t).now()`), `Deep.diff` of the event against `{name, at = t, bucket = BUCKETS[name], sessionId, playerId, fields}` is empty - so a missing, wrong OR extra field fails by name; omitted `playerId` is nil and omitted `fields` is `{}` | AC-1 |
| `AC-2: BUCKETS names all nine B6 events with their settled buckets, nothing else, and is frozen` | each of the nine present with the settled bucket; exactly 9 entries; every value one of D1/D2_7/D8_28; a write to `join` and a write to a new key both raise and leave the table unchanged | AC-2 |
| `AC-3: an event name not in BUCKETS is refused at construction, by name, and every B6 name still constructs` | six strangers (`joins`, `Join`, `round_complete ` with a trailing space, `purchase_refund`, `level_up`, `""`) each raise, and the message contains the name (except for `""`); all nine B6 names construct | AC-3 |
| `AC-6: a fields value that is not a string, number or boolean is refused naming the offending key, and every scalar is carried intact` | 5 refused cases (nested table, empty table, function, thread, a table among three scalars) each raise with the key in the message; 10 accepted cases (string, `""`, number, 0, negative, true, false, one of each, `{}`, nil) construct with fields deep-equal | AC-6 |

| Test (`telemetry_sink_test.luau`) | Asserts | AC |
|---|---|---|
| `Contract: Sink exports noop, recording and dispatch as plain field functions` | the three exports are functions | Contract |
| `AC-4: a recording sink holds exactly the three events emitted to it, in order, with fields intact, and clear() empties it` | fresh sink has 0 events; after three `sink.emit(e)` the list deep-equals the three positionally (order AND fields); a second `recording()` is empty (no shared list); `sink:clear()` empties; one emit after clear is held alone | AC-4 |
| `AC-4: dispatch through a working sink delivers zero, one and three events with none dropped, and the recording sink holds them in order` | `Sink.dispatch(recording, batch)` returns `delivered = #batch, dropped = 0` for 0, 1, 3 events and the sink holds the batch in order; same counts through `Sink.noop()` | AC-4, AC-5 positive half |
| `AC-5 (no propagation): a sink whose emit raises does not raise through Sink.dispatch to the caller` | `pcall(Sink.dispatch, raisingSink, events)` is `ok` for: raises on every event, raises on the second of three, raises a table value | AC-5 (a) |
| `AC-5 (counted drop): a raising emit is recorded as exactly one drop, the other events are still delivered, and delivered + dropped is the batch` | four cases: always-raising over 3 -> `0/3`; raise on 2nd of 3 -> `2/1` and events 1, 3 delivered in order; raise on 1st of 3 -> `2/1` and events 2, 3 delivered; raise on the only event -> `0/1`. A dispatch that raised instead of returning fails this half too, in those words | AC-5 (b) |

The controls suite is enumerated in `## Handoff` with its measured numbers.

**Deliberately not written.** No test that every name in `BUCKETS` has an
emitter - three are M5 by design and the story says not to. No private scan of
the two new modules for a clock or random source: `tests/shared/source_guard_test.luau`
scans every source module `scripts/classify.sh --list source` returns, checked
in this RED, so `src/shared/telemetry/*.luau` is covered the moment it exists,
and a second copy is how scanners drift (rules.md, "A test that needs this
answer asks for it").

## Handoff: RED -> GREEN

**Dispatch.** RED ran on **claude-fable-5-1** (`fable`), which is what
`## Model guidance` planned. Whether that came from the agent file's `model:`
or from an explicit per-dispatch override is not visible from inside the
dispatch; the orchestrator records which under `## Model guidance`.

### Files written

| File | What it is |
|---|---|
| `tests/helpers/TelemetryContract.luau` | the checks, one per criterion (AC-5 is two), over any Event / Sink module; the settled `BUCKETS` table; hand-built `TelemetryEvent` fixtures for the sink checks |
| `tests/helpers/TelemetryStubs.luau` | factory over one-field defect tables: baseline Event and Sink plus 8 + 5 wrong ones |
| `tests/shared/telemetry_event_test.luau` | 5 tests against the real `Event.luau`. **All red.** |
| `tests/shared/telemetry_sink_test.luau` | 5 tests against the real `Sink.luau`. **All red.** |
| `tests/shared/telemetry_controls_test.luau` | 15 control tests. **All green in RED** - they need only `Clock.manual` (ROUND-001, merged) and `Deep` (a test helper). |
| `.claude/tests/project-counters.test.sh` | `BASE_FORMAT`/`BASE_LINT` 48 -> 53 and the matching labels, per that file's own header; see `## Regressions` |
| `docs/backlog/stories/TEL-001.md` | this section, `## Test plan`, `## Regressions` |

No production file was written. No dependency was added (`## Contract` says
none; none was needed). No contract block was amended.

### The command, and the verbatim failure

    lune run test

Ten failures, all in the two real suites; every other file green, controls
included (`221 passed, 0 failed` on arrival; `236 passed, 10 failed` now: 25
new cases, 15 green controls, 10 red):

      FAIL  tests/shared/telemetry_event_test.luau :: AC-1: an event built with the injected clock's t carries at = t and exactly the supplied name, bucket, sessionId, playerId and fields
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_event_test:32: src/shared/telemetry/Event.luau did not load: error requiring module "../../src/shared/telemetry/Event": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_event_test.luau :: AC-2: BUCKETS names all nine B6 events with their settled buckets, nothing else, and is frozen
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_event_test:32: src/shared/telemetry/Event.luau did not load: error requiring module "../../src/shared/telemetry/Event": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_event_test.luau :: AC-3: an event name not in BUCKETS is refused at construction, by name, and every B6 name still constructs
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_event_test:32: src/shared/telemetry/Event.luau did not load: error requiring module "../../src/shared/telemetry/Event": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_event_test.luau :: AC-6: a fields value that is not a string, number or boolean is refused naming the offending key, and every scalar is carried intact
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_event_test:32: src/shared/telemetry/Event.luau did not load: error requiring module "../../src/shared/telemetry/Event": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_event_test.luau :: Contract: Event exports BUCKETS as a table and new as a plain field function
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_event_test:32: src/shared/telemetry/Event.luau did not load: error requiring module "../../src/shared/telemetry/Event": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_sink_test.luau :: AC-4: a recording sink holds exactly the three events emitted to it, in order, with fields intact, and clear() empties it
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_sink_test:28: src/shared/telemetry/Sink.luau did not load: error requiring module "../../src/shared/telemetry/Sink": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_sink_test.luau :: AC-4: dispatch through a working sink delivers zero, one and three events with none dropped, and the recording sink holds them in order
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_sink_test:28: src/shared/telemetry/Sink.luau did not load: error requiring module "../../src/shared/telemetry/Sink": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_sink_test.luau :: AC-5 (counted drop): a raising emit is recorded as exactly one drop, the other events are still delivered, and delivered + dropped is the batch
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_sink_test:28: src/shared/telemetry/Sink.luau did not load: error requiring module "../../src/shared/telemetry/Sink": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_sink_test.luau :: AC-5 (no propagation): a sink whose emit raises does not raise through Sink.dispatch to the caller
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_sink_test:28: src/shared/telemetry/Sink.luau did not load: error requiring module "../../src/shared/telemetry/Sink": could not resolve child component "telemetry"
      FAIL  tests/shared/telemetry_sink_test.luau :: Contract: Sink exports noop, recording and dispatch as plain field functions
            C:\Users\ryanc\Projects\first-roblox\tests\shared\telemetry_sink_test:28: src/shared/telemetry/Sink.luau did not load: error requiring module "../../src/shared/telemetry/Sink": could not resolve child component "telemetry"
    236 passed, 10 failed

**Why this is the right failure.** All ten fail at the `pcall`'d require - the
modules do not exist, which is the first thing the story requires - and each
names the module that did not load rather than the file being one `LOAD FAIL`
line. Not one fails on a helper, a syntax error or a timeout. It follows that
**no assertion in either real suite has executed**; what has executed is every
check in `TelemetryContract.luau`, against thirteen wrong modules and two right
ones, in the controls suite - which is the point of the arrangement.

`bash scripts/gates.sh --fast`: **format PASS (53), lint PASS (53), typecheck
PASS (9), build PASS; unit FAIL (the ten above); harness FAIL on one assertion
only** - "the working tree carries no stray .luau files", which is the five
untracked test files and clears on the RED commit. See `## Regressions`.
`coverage` is unconfigured. `--fast` is a read, not a record.

**Timings.** Local only; there is no CI number for this suite yet. The whole
`unit` gate took 26 s locally for 246 cases, and the 25 telemetry cases are
pure table work with no I/O and no loop over a generated collection; the runner
has no per-test timeout to budget against. Nothing here needs a budget.

### The export shape the tests already pin

Nothing below is a suggestion. Each name is already called by a test through the
checks in `TelemetryContract.luau`, so a wrong guess is a red test rather than
a debate.

    src/shared/telemetry/Event.luau                        (module returns a table)

      Event.BUCKETS : { [string]: Bucket }
        - exactly NINE entries: join D1, first_round_complete D1,
          bounce_before_resolution D1, session_end D1, round_complete D2_7,
          rematch_accepted D2_7, day_n_return D8_28,
          co_play_with_known_player D8_28, purchase D8_28
        - every value is one of "D1" | "D2_7" | "D8_28"
        - FROZEN as observed by a caller: `BUCKETS.join = "D8_28"` raises and
          leaves "D1"; `BUCKETS.not_a_b6_event = "D1"` raises and the key stays
          nil. `table.freeze` satisfies both; a `__newindex` metatable alone
          does NOT (it lets the existing-key write through).

      Event.new(name: string, at: number, sessionId: string,
                playerId: string?, fields: { [string]: string | number | boolean }?)
        -> TelemetryEvent
        - a plain field function, called `Event.new(...)` with a dot
        - returns a table with EXACTLY these keys and no others (AC-1 is a
          Deep.diff, so an extra key such as `kind` or `_id` fails by name):
              name, at, bucket, sessionId, playerId, fields
        - `at` is the number passed in, unchanged (0, 1234.5, 2^31 all round-trip)
        - `bucket` is `BUCKETS[name]`
        - `playerId` nil when omitted; `fields` is an EMPTY TABLE when omitted
        - `fields` deep-equals what was passed (a copy or the same table - not
          constrained)
        - RAISES when `name` is not a key of BUCKETS; the error message must
          CONTAIN the offending name verbatim (`"joins"`, `"Join"`,
          `"round_complete "` with its trailing space, `"purchase_refund"`,
          `"level_up"`); for `""` only the raise is required
        - RAISES when any `fields` value is not a string, number or boolean
          (tested: table, empty table, function, thread); the error message
          must CONTAIN the offending key verbatim (`payload`, `extra`,
          `callback`, `job`, `nested`)
        - accepts `false`, `0`, `""` and negative numbers as values

    src/shared/telemetry/Sink.luau                         (module returns a table)

      Sink.noop() -> { emit: (TelemetryEvent) -> () }
        - `emit` is a plain field function: `sink.emit(event)`, no self

      Sink.recording() -> RecordingSink
        - `.emit(event)` plain field function, dot call
        - `.events : { TelemetryEvent }` - a list, empty on creation, one
          list PER SINK (two recording sinks do not share it), appended in
          emission order, each entry deep-equal to what was emitted
          (fields included; same table or a copy - not constrained)
        - `:clear()` a METHOD, colon call, empties `.events`

      Sink.dispatch(sink: Sink, events: { TelemetryEvent }) -> DispatchResult
        - `DispatchResult.delivered : number`, `DispatchResult.dropped : number`;
          the checks read these two fields only (an extra field is not
          constrained, but the Contract names only these two)
        - NEVER raises: each `emit` is isolated (pcall) - a raise on one event,
          whether a string or a table error value, does not reach the caller
          and does not stop the events after it
        - `delivered` = emits that returned, `dropped` = emits that raised,
          `delivered + dropped == #events`; `{}` -> 0/0

    Not constrained, and therefore GREEN's choice: the wording of the error
    messages beyond containing the name / key; the `error` level; whether
    `new` validates the TYPES of `name`, `at`, `sessionId` (the tests always
    pass a string, a number, a string); what happens to a non-string KEY in
    `fields` (AC-6 is about values; the Contract's type says string keys);
    whether `fields` is copied; what `noop().emit` returns; any type
    annotations or exported types (the tests are untyped over `any`).

### Negative controls: expected values, measured in RED

Every check was executed in RED against the stubs in `TelemetryStubs.luau` and
the numbers below are what it reported, not predictions. Each row's control is
the baseline with ONE defect. "Still passes" is asserted in the same test, so a
stub that failed everything would fail the controls suite.

| Control (stub) | Built to fail | Measured failing cases | Still passes |
|---|---|---|---|
| `defaultUnknownToD1` (AC-3's named control; mutation 1) | AC-3 | 6 of 6 strangers, each `returned an event with bucket "D1"` | AC-1, AC-2, AC-6 |
| `missingPurchase` (mutation 3) | AC-2 | AC-2: `1 of 9 wrong or missing`, `8 entries in all`; **also** AC-1 `1 of 5` (the purchase case raised) **and** AC-3 `1 case` (`purchase` refused as a B6 name) | AC-6 |
| `extraTenthEvent` | AC-2 | AC-2: `1 extra ... (10 entries in all, 9 expected)`, `level_up = "D1" is not a B6 event`, `0 way(s) it is not frozen`; **also** AC-3 `1 case` (`level_up` is a stranger and constructed) | AC-1, AC-6 |
| `bounceInD2_7` | AC-2 | AC-2: `1 of 9 wrong`, `bounce_before_resolution: expected D1, got "D2_7"`; **also** AC-1 `1 of 5` (`value.bucket: expected "D1", got "D2_7"`) | AC-3, AC-6 |
| `unfrozenBuckets` | AC-2 | `2 way(s) it is not frozen` (existing key and new key both written) | AC-1, AC-3, AC-6 |
| `acceptNestedTable` | AC-6 | `3 of 15` (the three table cases; function and thread still refused) | AC-1, AC-2, AC-3 |
| `refuseWithoutKey` | AC-6 | `5 of 15` (every non-scalar case: refused, key not named; no scalar case) | AC-1, AC-2, AC-3 |
| `atIsZero` | AC-1 | `4 of 5` (`value.at: expected 1234.5, got 0` etc.; t = 0 indistinguishable) | AC-2, AC-3, AC-6 |
| `propagateEmitError` (AC-5's first named control) | AC-5 no-propagation | `3 case(s)` - every raising sink reached the caller, table error included; **also** AC-5 counted-drop `4 of 4`, `dispatch raised instead of returning a result, so nothing was counted` | AC-4, dispatch |
| `countRaiseAsDelivered` (AC-5's second named control; mutation 2) | AC-5 counted-drop **alone** | `4 of 4`, `dropped = 0` every case, e.g. `raises on every event: dropped = 0, expected 3; delivered = 3, expected 0` | AC-4, dispatch, **AC-5 no-propagation** (asserted to pass explicitly) |
| `stopAtFirstError` | AC-5 counted-drop alone | `3 of 4` (`raises on every event: dropped = 1, expected 3`; the only-event case indistinguishable) | AC-4, dispatch, AC-5 no-propagation |
| `recordingOutOfOrder` | AC-4 | slots 1 and 3 swapped by name; slot 2 not reported; **also** `dispatch` (reads the sink back) | AC-5 both halves |
| `recordingDropsFields` | AC-4 | `value.N.fields: expected {...}, got nil` for all three plus the one after clear; **also** `dispatch` | AC-5 both halves |
| `correctEvent` / `correctSink` (baselines) | nothing | - | every check |

**Confirming these against the shipped module is GREEN's job.** The real suite
has not executed one assertion; when it does, GREEN should see all ten tests
green with no check edited, and the orchestrator's mutations below should
reproduce the counts in the next table.

### Mutation table for the orchestrator (predicted from the measured controls)

| `## Notes` mutation, applied to the shipped module | Predicted red in `telemetry_*_test.luau` | Basis |
|---|---|---|
| 1. `Event.new` defaults an unknown name to `"D1"` | **1 test**: `AC-3: ...` alone; message shows 6 strangers with bucket "D1" | `defaultUnknownToD1` measured AC-3 alone |
| 2. `Sink.dispatch` counts a raising `emit` as `delivered` | **1 test**: `AC-5 (counted drop): ...` alone, `4 of 4 cases`, `dropped = 0`; `AC-5 (no propagation)` stays green | `countRaiseAsDelivered` measured counted-drop alone - the single-assertion case, as the story predicts |
| 3. Remove `purchase` from `BUCKETS` | **3 tests**: `AC-2` (`purchase: expected D8_28, got nil (missing)`, `8 entries in all`), AND `AC-1` (the purchase construction case raises), AND `AC-3` (`purchase` refused as a B6 name). The story's `## Notes` predicts "AC-2 goes red"; it does, and two more with it, because AC-1 and AC-3 both construct every B6 name. That is a property of the defect, measured on the stub, not a looseness in AC-2. | `missingPurchase` measured AC-1, AC-2, AC-3 |

Extra mutations the controls also earn, if wanted: propagate the raise from
`dispatch` -> 2 tests (both AC-5 halves); accept a table in `fields` -> AC-6
alone; file `bounce_before_resolution` under D2_7 -> AC-1 and AC-2.

### The caller check, re-run against the tree

Re-run in this RED, not copied forward:

    $ git ls-files src lune | xargs grep -ln 'telemetry\|TelemetryEvent\|Bucket\|Sink'
    (no output)
    $ git ls-files --cached --others --exclude-standard tests | xargs grep -ln 'telemetry\|TelemetryEvent\|Bucket\|Sink'
    tests/helpers/TelemetryContract.luau
    tests/helpers/TelemetryStubs.luau
    tests/shared/telemetry_controls_test.luau
    tests/shared/telemetry_event_test.luau
    tests/shared/telemetry_sink_test.luau
    $ ls src/shared/telemetry
    ls: cannot access 'src/shared/telemetry': No such file or directory

No source or runner file references either module; the only hits are the five
files this RED wrote. The hazard the section exists for cannot arise.

### The source guard, checked and not duplicated

`tests/shared/source_guard_test.luau` asserts AC-5 of ROUND-001 over
`SourceScan.sourceFiles()`, which is `scripts/classify.sh --list source` over
the whole tree (uncommitted files included - its own AC-6 test proves it). So
`src/shared/telemetry/Event.luau` and `Sink.luau` are scanned for `os.clock`,
`tick`, `os.time`, `math.random` and the rest from the moment GREEN writes them.
No second scan was written. If GREEN reaches for a clock inside `Event.new`,
that guard - not this story's tests - is what goes red, by file and line.

### Deferred verifications

The story defers none (the section's comment says so). Nothing here to decline.

### What GREEN must not do

- Edit any of the five test files. If a check is wrong, return to RED and say
  so under `## Regressions`.
- Change the six strangers, the five rejected field cases, or the settled
  `Contract.BUCKETS` table - those are the input domain the criteria claim to
  hold over.
- Make AC-2's frozen check pass with a `__newindex` metatable alone; the
  existing-key write must raise too.
- Satisfy AC-5 by catching in the RECORDING sink; the isolation belongs in
  `Sink.dispatch`, which is what the checks drive with a hand-built raising
  sink that has no pcall of its own.
- Raise the `unit` floor here: `## Notes` gives that to the Lead PO at GATES.
  The new real count will be **246** (`236 passed` + 10 red) once green.

### Notes for the implementer

- `## Contract` is buildable as written; nothing was amended. `fields: {...}?`
  on `new` with a non-optional `fields` on the type is why the omitted case
  must produce `{}` - AC-1's last case pins it.
- Message wording is free, but the AC-3 and AC-6 checks use a plain
  `string.find(message, needle, 1, true)` for the name / key, so
  `tostring(name)` must appear verbatim - a message that quotes only a
  truncated or lower-cased name fails `"Join"` and `"round_complete "`.
- `Deep.diff` treats `playerId = nil` and an absent `playerId` identically,
  so either is fine for the no-player case.
- The stubs in `TelemetryStubs.luau` are NOT a reference implementation; they
  are untyped, message-arbitrary, and exist to be wrong. Build from the
  Contract.

## Regressions

### `.claude/tests/project-counters.test.sh`: literals moved 48 -> 53 (RED)

The `harness` gate (required) counts `.luau` files through the gates' own
evidence lines and pins the count as literals. This RED added five test files
and no source, so `BASE_FORMAT`/`BASE_LINT` moved 48 -> 53; `BASE_TYPECHECK`
(9) and `NARROW_*` (9 / 9 / 5) are untouched because nothing under `src/`
changed. The file's own header prescribes this in RED, and SEAT-002's RED did
the same (its `## Regressions`). The numbers were read from the gates' evidence
lines in `gates.sh --fast` - `stylua over 53 files`, `selene over 53 files`,
`analyze over 9 files` - and the narrow ones from the same commands with the
target replaced (`stylua over 9 files`, `selene over 9 files`, `analyze over 5
files`); not counted by hand.

**Before**, from `.claude/state/gate-logs/harness.log` of the first
`gates.sh --fast`, with the five files present and the literals at 48:

        FAIL the working tree carries no stray .luau files, so the baselines mean what they say
        FAIL format reports 48 files on the unmodified tree
             expected count: 48
             actual count:   53
        FAIL lint reports 48 files on the unmodified tree
             expected count: 48
             actual count:   53
        FAIL AC-2: format counts the untracked file (48 -> 49)
             expected count: 49
             actual count:   54
        FAIL AC-4: lint counts the untracked file (48 -> 49)
             expected count: 49
             actual count:   54
        FAIL format does not count the ignored file (still 48)
             expected count: 48
             actual count:   53
        FAIL lint does not count the ignored file (still 48)
             expected count: 48
             actual count:   53
    project-counters: 33 passed, 7 failed

**After** the literals and labels moved:

    $ bash .claude/tests/project-counters.test.sh
        FAIL the working tree carries no stray .luau files, so the baselines mean what they say
             expected:
             actual:   ?? tests/helpers/TelemetryContract.luau
    project-counters: 39 passed, 1 failed

The one remaining failure is the stray-file precondition: the five test files
are untracked until the RED commit, and `git status --porcelain` lists them. It
clears on a clean tree; the orchestrator should see `40 passed, 0 failed` after
committing, as SEAT-002 did.
