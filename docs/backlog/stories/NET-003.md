---
id: NET-003
title: A remote is rate-limited per player against the injected clock
slug: a-remote-is-rate-limited-per-player-agai
epic: EPIC-02
type: feature
status: in-review
phase: REVIEW
branch: story/NET-003-a-remote-is-rate-limited-per-player-agai
depends_on: [NET-002]
required_gates: []
---

## Context

The fifth and last check of the pipeline, and B4's fourth adversarial case: a call
arriving at a hundred per second. Without it, every remote is a denial-of-service
surface and every in-game budget is advisory.

It has a second job that is not defensive. `tuning.md` §3 gives
`signal_rate_limit_seconds` = 1.5 as **derived**, serving two purposes at once: it
keeps the shared signal stream readable at `players_max` senders, and it satisfies
B4's requirement that every remote be rate-limited. The same mechanism is a game
rule and a security control, which is unusual and worth knowing — it means a story
that "relaxes the rate limit for responsiveness" is changing the game's difficulty.

The limiter is driven by the **injected clock** from `ROUND-001`, so the whole of
it is tested in simulated time with no waiting. A rate-limit test that sleeps is a
slow test that eventually flakes on CI hardware.

**Which required gate would fail if this story's artifact broke:** `unit`.

## Acceptance criteria

- **AC-1** — Given a remote with `minIntervalSeconds = 1.5` and a clock at `t`,
  when a seated player calls it at `t`, then it is accepted; when the same player
  calls again at `t + 1.4`, then it is rejected with reason `"rate"`; and when they
  call at `t + 1.5`, it is accepted.
  *Semantics:* the comparison is `now - lastAccepted >= minIntervalSeconds`,
  inclusive. Exactly at the interval the call goes through.
- **AC-2** — Given two players, when player A is rate-limited, then player B's call
  at the same instant is accepted.
  *Control:* an implementation with one shared last-call timestamp passes AC-1 and
  **must** fail this. It is a one-word difference in the implementation and it would
  make one fast player mute everybody else.
- **AC-3** — Given a rejected call, when the next call from that player arrives,
  then the interval is measured from the last **accepted** call, not the last
  attempted one.
  *Control:* an implementation that updates the timestamp on rejection **must** fail
  this — under it, a player calling continuously at 100 Hz is locked out forever
  rather than being served every 1.5 seconds. That turns a rate limit into a
  self-inflicted denial of service.
- **AC-4** — Given a remote declared with no `rateLimit`, when it is called
  repeatedly, then every call is accepted.
- **AC-5** — Given a call that fails identity, shape, range or phase, when it is
  guarded, then the limiter's state for that player is **not** advanced.
  *Semantics:* rate is check 5 of 5. A malformed flood must not consume the
  attacker's own budget in a way that would also throttle their legitimate calls —
  and more importantly, must not be silently absorbed before `TEL-003` can count
  it as malformed.
- **AC-6** — Given a player who leaves the round, when the limiter is asked to
  forget them, then their entry is removed, and the limiter's stored entry count
  after a full round of joins and leaves equals the number of currently seated
  players.
  *Semantics:* the limiter keys on player id and a server runs for days. An
  unbounded table keyed by every player who ever connected is a memory leak with a
  slow fuse.

## Contract

Every block below is pinned by the Lead PO at PLANNED. **RED may amend a block in
place, with a reason written beside it, and GREEN builds what the amended block
says.** That is not an `## Amendments` entry — that section is for acceptance
criteria, which are frozen from the moment this story leaves PLANNED. The
contract is the shape; the criteria are the promise.

### What changes

| File | Why | Phase |
|---|---|---|
| `src/net/RateLimiter.luau` | the limiter itself — new file | GREEN |
| `src/net/Wrapper.luau` | check 5, after phase | GREEN |
| `tests/net/**`, `tests/helpers/**` | the suite | RED |
| `.claude/tests/project-counters.test.sh` | the `.luau` file-count literals move for the new files | RED |

No change to `src/net/Remotes.luau`: `RateLimit` and the optional `rateLimit`
field were declared by `NET-001` and are already exactly right.

### `src/net/RateLimiter.luau`

    export type RateLimiter = {
        allow:  (self: RateLimiter, playerId: PlayerId, key: string, now: number) -> boolean,
        forget: (self: RateLimiter, playerId: PlayerId) -> (),
        size:   (self: RateLimiter) -> number,          -- entries held; AC-6 reads this
    }

    RateLimiter.new(minIntervalSeconds: number) -> RateLimiter

`key` is the remote name, so one player's limit on one remote is independent of
their limit on another. `now` is passed in; the limiter never reads a clock, and
`ROUND-001`'s source guard would catch it if it did — `os.clock`, `tick` and
`math.random` are refused in every `src/` module but their one adapter, and the
guard enumerates through the classifier, so it covers this file the moment it
exists without anyone adding it to a list.

**`allow` records the timestamp only when it returns `true`.** That single line is
AC-3.

**`PlayerId` is re-declared here, not imported from `Wrapper`.** `Wrapper` will
require `RateLimiter`, and the reverse edge would be a cycle. It is the same
string alias.

### What the limiter stores, and what `size()` counts

**One entry per PLAYER, holding a timestamp per key.** Not one entry per
`(player, key)` pair:

    entries: { [PlayerId]: { [string]: number } }
    size()  -> the number of PLAYERS held
    forget(playerId) -> removes that player's whole entry, every key with it

> **Amended in RED** (2026-09-22). Two consequences of this shape are pinned
> by the AC-6 check, stated here so GREEN meets them on purpose rather than by
> accident: (1) **`forget` makes the player new again** - a call by that player
> at the very instant they were just refused is allowed, because there is no
> entry to measure from; a `size()` that decrements a counter while leaving the
> timestamp behind would satisfy the count and fail this. (2) **`forget` of a
> player the limiter never saw is a no-op**, not a raise, and does not change
> `size()`: a disconnect can arrive for a player who never called anything.
> *Reason:* AC-6 says "their entry is removed", and (1) is what removal means
> from the outside; `size()` alone can be satisfied by bookkeeping. Nothing in
> the criteria moved.

This is pinned because AC-6 is phrased in terms of it and the two shapes are a
one-line difference that no other criterion can tell apart: *"the limiter's stored
entry count after a full round of joins and leaves equals the number of currently
seated players"* is true under this shape whatever mix of remotes each player
called, and false under a `(player, key)` table for any player who used two
remotes. A `size()` that counts pairs would make AC-6 read as a statement about
how many remotes the test happened to exercise, which is not a property of
anything. `forget` taking a player and dropping all their keys is the same
decision seen from the disconnect side — a leaver stops existing, and "per
remote" is not a question anyone asks about a player who has gone.

### How `Wrapper.guard` obtains a limiter — and why it is not injected

`guard` constructs **one limiter per `guard()` call**, in the enclosing scope of
the returned closure, when and only when `definition.rateLimit ~= nil`:

    local limiter = if definition.rateLimit ~= nil
        then RateLimiter.new(definition.rateLimit.minIntervalSeconds)
        else nil
    return function(playerId, rawArgs) ... end

so its state persists across calls to one guarded function — a limiter built
*inside* the closure would be new on every call and could never reject anything.
The key passed to `allow` is `definition.name`.

**Not through `GuardContext`, and this was checked rather than assumed.** Adding
a `limiter` field to `GuardContext` is the design that lets a driver call
`forget` on a disconnect, and it is the one this story does **not** take, for a
reason measured against the tree on 2026-09-22: every `GuardContext` in
existence is built by two frozen test helpers — `NetContract.context()`
(`NetContract.luau:133`) and `PhaseContract.world()` (`PhaseContract.luau:117`)
— and neither may be edited outside RED. A required field would make `guard`
index a nil on all of `NetContract`'s and `PhaseContract`'s checks; an optional
one is a signature change with a caller list, which this story's scope denies.
The tests are not typechecked either (the `typecheck` gate runs over `src` only),
so the failure would arrive at runtime across two merged stories rather than as
a compile error.

**The consequence, stated rather than discovered later:** under this design
`forget` and `size` are reachable from tests and from a future driver that holds
its own limiter, but **no current production path calls them**. AC-6 is a unit
criterion on `RateLimiter`, which is exactly how it is phrased ("when the limiter
is asked to forget them"). Wiring a disconnect to `forget` belongs to the M3
driver and to `SEAT-003`, and until then the leak AC-6 guards against cannot
occur, because nothing in production holds a limiter across rounds. This is the
same "declared early, consumed later" move `NET-001` made with `legalPhases` and
`rateLimit` themselves, and it is recorded here so the next story does not read
`forget`'s absence from production as an oversight.

### Change to `src/net/Wrapper.luau`

The rate check runs **last**, after phase, and is check 5 of the fixed order the
module header already documents. **No signature change** to `Wrapper.guard`,
`GuardContext` or `Rejection` — `"rate"` is already in the reason union and
`clock` is already in `GuardContext`, both put there by `NET-001` for this story.
See `## Callers of changed signatures`: there is no changed signature, and that
was grepped rather than assumed.

### `context.clock.now()` is read per call that reaches check 5, and not before

The parallel of `NET-002`'s read-count pin, one position later, and the reason is
the same: *"after phase"* is only observable if something counts. For a remote
that **declares** a `rateLimit`:

- `context.clock.now()` is read **0 times** on a call identity, shape, range or
  phase has already rejected. This is AC-5's structural form: a limiter that is
  never consulted cannot have advanced.
- it is read **at least once** on a call that reaches check 5, and the value
  compared is the one that call returned — not one captured when `guard` was
  called. A captured clock is `NET-002`'s captured phase wearing a different hat,
  and it passes every test that never advances time.

For a remote with **no** `rateLimit`, `context.clock.now()` is read **0 times,
ever**: AC-4's structural form. Not constrained: whether the clock is read during
`guard()` construction (it must not be *compared*, but reading it is not
forbidden), or read more than once per call.

### `detail` for a rate rejection

As with `NET-002`'s phase rejection, the wrapper builds this `Rejection` itself —
no schema is in the path — so **`detail` carries no `"rate: "` prefix**. `reason`
already names the check that fired, and a second encoding is one a later refactor
can let drift.

What it must carry, because `TEL-003` emits it and an operator reads it when a
spike appears: **the remote's name and the interval that was required.** The
pinned case: a remote declared `{ minIntervalSeconds = 1.5 }` under a unique
name, called twice inside the interval — `detail` is non-empty, does **not** match
`^rate:`, contains the remote's name as a plain substring, and contains `1.5`.
Wording otherwise free.

*Deliberately not pinned:* how long the caller actually waited. It is the obvious
second number and it is a float difference (`t + 1.4` minus `t` is not exactly
`1.4` in binary floating point), so a substring assertion on it would pin the
formatting of a rounding error. An implementation is free to include it.

### Where 1.5 comes from, and where it must NOT be put

`1.5` is `tuning.md` §3's `signal_rate_limit_seconds`, marked **derived**. AC-1
calls it settled: read it out, do not tune it.

**It enters the test through the declaration** —
`rateLimit = { minIntervalSeconds = 1.5 }` on the remote the test declares, read
back out of `definition.rateLimit.minIntervalSeconds` — and **not** through
`src/shared/Tuning.luau`. Adding it there would break a frozen guard, which was
measured rather than assumed: `Tuning`'s scope is `tuning.md` **§1 and §5 only**
(`Tuning.luau:14`), and `TuningSpec.luau:420-438` fails any module key its parse
of those two sections does not name — *"in the module, named nowhere in
`tuning.md` §1 or §5"*. A §3 constant added to `Tuning` reds `tuning_spec_test`
and `tuning_controls_test`, which are `ROUND-002`'s and frozen. This story
introduces no tuning constant and changes none, as `## Game design` says.

**One provenance assertion, and its vacuity checks.** So that the `1.5` in the
suite is anchored to the document rather than to somebody's memory, RED asserts
that `tuning.md` §3 still names `signal_rate_limit_seconds` with the value the
tests declare. It is one test, and the vacuity half is the point — an extraction
that matches nothing compares nothing and passes:

1. exactly **one** table row in `tuning.md` has `` `signal_rate_limit_seconds` ``
   as its **first cell** (not zero, which is the failure mode, and not two);
   *Amended in RED* from "names" - the name also appears in `mechanics.md`,
   `playtest.md` and could appear in another row's rationale cell, and a
   substring match over the document would count those. First-cell anchoring
   is what makes "a row" mean the row that fixes the value;
2. the value cell yields a **number**, asserted to be a number before it is
   compared;
3. that number equals the `minIntervalSeconds` the suite declares.

`TuningSpec.parse` cannot be reused: it collects §1 and §5 only, by the same
`SECTION_TABLE` filter that makes the paragraph above true. A small local reader
is correct here. State the limit the scan has, as `SourceScan` and `PhaseUnion`
both do: it reads the table row as it is written today.

### Why an interval and not a token bucket

A minimum interval is what `tuning.md` specifies (`signal_rate_limit_seconds`),
and it is the right shape for this game: a token bucket permits a burst, and a
burst of signals is exactly the thing the constant exists to prevent — the shared
stream stops being readable at `players_max` senders. Scarcity is handled
separately and far more strictly by `signal_budget_per_player`, which is the
binding constraint "by two orders of magnitude". If a later remote wants burst
tolerance, that is a new limiter alongside this one, argued in its own story.

### Oracle partition

| AC | Kind | Instruction to RED |
|---|---|---|
| AC-1 | **Settled** | 1.5 is `tuning.md` §3's `signal_rate_limit_seconds`, **derived**. Read it out; do not tune it. Through the declaration, never through `Tuning` — see the block above, which says what breaks. The boundary case (`>=`, accepted exactly at the interval) is the half a `>` implementation passes everything else with. |
| AC-2, AC-3, AC-5, AC-6 | **Mechanical, with controls** | The controls are in the criteria. AC-3's is the one that matters most: it describes a bug that only appears under load, which is the only condition the code exists for. |
| AC-4 | **Mechanical** | Pin exactly, including its structural form: a remote with no `rateLimit` never reads the clock. |
| the `detail` and read-count blocks | **Mechanical** | Contract, not criteria. Pin them as `NET-002` pinned its two: one test each, named `Contract: ...`. |
| the `tuning.md` §3 provenance | **Oracle-free** | You are inventing the reader. `PhaseUnion.luau` and `raw_remote_guard_test.luau` are the shape, vacuity assertions included. Three of them, listed above; the first is not optional. |

### The controls this story needs

Follow `NET-002`'s pattern exactly, which worked: checks in a `RateContract.luau`
parameterised over a `{ Schema, Remotes, Wrapper, RateLimiter }` bundle, defective
bundles in a `RateStubs.luau` **layered over `PhaseStubs.correct()`** (which is
already layered over `NetStubs.correct()`), the real suite against `src/net/`, and
a controls suite that needs nothing from GREEN and therefore RUNS in RED. Do not
edit `NetStubs.luau` or `PhaseStubs.luau`: they are frozen, and layering is why
they do not need to change.

Each control is **one defect**, and each is asserted to fail the checks it is
built for **and pass the rest** — that is what makes "fails N of M" evidence
rather than decoration. At least these, named in the criteria or in this contract:

| Control | Named by | Should fail |
|---|---|---|
| `sharedTimestamp` — the wrapper passes one player id to `allow` for everybody, so one `lastAccepted` serves all callers | **AC-2** | AC-2 alone |
| `recordsOnReject` — `allow` records whether or not it permits | **AC-3** | AC-3, **and AC-1** (see the amendment below) |
| `strictlyGreater` — `>` instead of `>=` | **AC-1** (the boundary) | AC-1 alone |
| `ignoresRateLimit` — no check at all (the real wrapper today) | — | the union that equals this story's red wrapper tests (AC-6 is red for a different reason: the module is absent) |
| `limitsWithoutRateLimit` — limits a remote that declared none | **AC-4** | AC-4, plus the three earlier checks that call one guard repeatedly at a frozen clock |
| `rateBeforePhase` — check 5 moved ahead of check 4 | **AC-5** | AC-5 and the read-count Contract check |
| `neverForgets` — `forget` is a no-op | **AC-6** | AC-6 alone |
| `sizeCountsKeys` — `size()` counts `(player, key)` pairs | **AC-6** (the shape pinned above) | AC-6 alone |
| `rejectsEverything` — the **wrapper** refuses every call reaching check 5, `rateLimit` or not | positive control for **AC-1**'s first call and **AC-4** | AC-1, AC-4, and every other check that accepts a call |
| `capturesClockAtGuard` — `context.clock.now()` read once at `guard()` | the read-count block | AC-1, AC-3, AC-5 and the read-count check |
| `prefixedDetail` — `"rate: ..."` | the `detail` block | the detail check alone |
| `detailOmitsInterval` | the `detail` block | the detail check alone |
| `detailOmitsName` | the `detail` block | the detail check alone |

> **Amended in RED** (2026-09-22), three rows, each against a measurement in
> `tests/net/rate_controls_test.luau`. (1) `recordsOnReject` cannot fail "AC-3
> alone": AC-1's own three calls put a rejection at `t + 1.4` and an acceptance
> at `t + 1.5`, 0.1 s apart, and a limiter that recorded the rejection refuses
> the acceptance - so any AC-1 test written from the criterion's text catches
> this defect. Measured: AC-1 and AC-3, 2 of 28. (2) `rejectsEverything` as
> "`allow` always false" could never reach AC-4 under this Contract's own guard
> design, because a remote with no `rateLimit` constructs no limiter and never
> calls `allow`. The control is therefore in the wrapper - the verdict at check
> 5 is always a rejection - which is the shape that fails both things it is the
> positive control for. (3) `sharedTimestamp` is written at the wrapper level
> (one id passed to `allow`) so that it is the one-check catch the criterion
> describes; the limiter-level variant `## Notes` calls mutation 2 ("key on
> `key` only, dropping `playerId`") also breaks `size()` and `forget`, and so
> reds AC-6 as well - predicted in the handoff's mutation table. Two controls
> were added beyond the eleven: `detailOmitsName` (the detail block's other
> clause) and `capturesClockAtGuard` (the read-count block's "not one captured
> when `guard` was called", NET-002's mutation 1 in a different hat).

`rejectsEverything` is the positive control: AC-1's first call and AC-4 are green
the moment the limiter is absent, so they are **not earned by RED's red** and
something must be able to fail them. Say so in the handoff, as `NET-002` did with
`rejectsEveryPhase`.

### No test in this story may sleep, wait, or read real time

The clock is injected precisely so that a hundred calls at 10 ms intervals is an
instant test. A rate-limit suite that takes eight seconds is a suite that starts
flaking on CI hardware, and this is the one story where writing `task.wait` would
feel natural. Time moves only through `Clock.manual`'s `advance` and `set`.

## Callers of changed signatures

**There is no changed signature, and that is a measurement rather than a claim.**
`RateLimiter` is a new module with no callers; `Wrapper.guard`, `GuardContext`,
`Rejection` and `Remotes.define` all keep the shapes `NET-001` gave them and
`NET-002` left untouched. What the caller list exists to catch — an existing
caller that no longer satisfies the callee — has one candidate here, and it is
**behavioural rather than structural**: a frozen test that guards a remote
declaring a `rateLimit` and calls it twice would start being rejected for
`"rate"` the moment this story's check exists, and neither the typecheck gate nor
RED would see it.

Checked against the tree on 2026-09-22, before dispatch, with
`grep -rn rateLimit src tests` and `grep -rn "\.guard(" src tests`:

- **Exactly one place in the whole tree declares a `rateLimit`:**
  `tests/helpers/NetContract.luau:768`, inside
  `defineReturnsTheDeclarationAndAllListsIt`. It asserts the field comes back off
  the definition (`:782`) and that a declaration without one has
  `rateLimit == nil` (`:803`). **It never guards that definition and never calls
  it.** So the one frozen caller that could have been broken by this story is
  not.
- **Every `Wrapper.guard` call site in the tree** — 10 in
  `tests/helpers/NetContract.luau` (lines 272, 316, 349, 382, 425, 456, 507, 598,
  672, 715) and 8 in `tests/helpers/PhaseContract.luau` (199, 231, 271, 358, 383,
  417, 520, 559) — passes a definition built **without** a `rateLimit`, so every
  one of them takes AC-4's path: no limiter, no clock read, every call accepted.
  Several call one guard repeatedly with one player at a clock frozen at `0`,
  which is precisely the shape that would break if a limiter were applied to a
  remote that declared none. That is the `limitsWithoutRateLimit` control, and it
  is why AC-4 is a criterion rather than an implementation detail.
- `tests/helpers/NetStubs.luau:309` and `:283` are the stub `Wrapper.guard` and
  stub `define`, not callers of the real ones.
- **No production source calls `Wrapper.guard` at all.** There is still no driver;
  `architecture.md`'s composition root is M3.

**RED's handoff must state that this list was re-checked against the tree.**
## Deferred verifications

<!-- Nothing is deferred. The template requires this section only when a
     verification provably cannot run in the phase that wants it, and says
     to omit it otherwise - so the word "None" in prose here reads to
     check-boundaries.sh as a deferred entry with no owner and no result,
     which is the same defect as a WAIVED with no reason.
     Planned text was: None. Everything here runs in simulated time.
-->

## Model guidance

<!-- plan.sh:generated:begin -->
Planned by `bash scripts/plan.sh write NET-003` from `.claude/harness/models.conf`.
A PLAN, not a record: a session setting or an explicit override can beat both
this and the agent's own `model:` field, and nothing here can see which won.
The orchestrator still writes down the model each dispatch **resolved** to, by
name, below the table. Only what lies BETWEEN these two markers is rewritten when
this command runs again; the rest of the section is yours and is preserved.

| Phase | Agent | Planned | Why |
|---|---|---|---|
| PLANNED | `lead-po` | `opus` | planning is the judgement phase: decomposition, the oracle partition, and what goes in the contract |
| RED | `test-developer` | `fable` | the measured case. With a partitioned contract to work from, the brief carries the judgement and the weaker model writes sharper negative controls than the stronger one did without it |
| GREEN | `feature-developer` | `opus` | the failure mode of a weaker model here is reaching green by weakening a test, which is the one thing this harness exists to prevent |
| GATES | `feature-developer` | `opus` | same risk as GREEN, and a gate failure is where "make it stop complaining" is most tempting |
| REVIEW | `lead-po` | `opus` | reading review feedback against the contract is judgement, and a wrong call here ships |
| SCAFFOLD | `lead-po` | `opus` | source, tests and config in one indivisible derivation, with no failing test in front of any of it |
<!-- plan.sh:generated:end -->

**Resolved model of every dispatch, by name:** PLANNED `lead-po` -> `claude-opus-5` (the orchestrating session). RED `test-developer` -> `claude-fable-5-1`, the planned weaker model, passed explicitly on the Agent call so the resolution is the orchestrator's act rather than an inference from the agent file; the dispatch confirmed the name from inside. GREEN and GATES `feature-developer` -> `claude-opus-5`, also passed explicitly. REVIEW `lead-po` -> `claude-opus-5`.

Brief RED explicitly: **no test in this story may sleep, wait or use real time.**
The clock is injected precisely so that a hundred calls at 10 ms intervals is an
instant test. A rate-limit suite that takes eight seconds is a suite that will
start flaking on CI hardware, and `project.conf`'s `ci-factor` note exists because
that has happened elsewhere.

## Out of scope

- Any global or per-server rate limit. Per player, per remote, and nothing else.
- Banning, kicking or escalation on repeated violation. A rejection is a rejection;
  what an operator does about a spike is an M5/M6 decision informed by `TEL-003`.
- Burst tolerance or token buckets. Argued against above.
- The signal budget (`signal_budget_per_player`). A game resource, not a rate
  limit, and M3.
- Client-side throttling. It would be a courtesy to honest clients and worth
  nothing against hostile ones.

## Game design

Implements the security half of `tuning.md` §3's `signal_rate_limit_seconds` (1.5,
**derived**) ahead of the signal remote that will use it. The constant's own
rationale names both purposes, so this story introduces no new constant and
changes none.

The design property to preserve: the limiter is **not a scarcity mechanism**. If a
later story finds itself tuning this number for game balance rather than for stream
readability, that is a signal the budget is doing the wrong job and belongs in a
conversation with the Game Designer, not in this module.

## Notes
### PO decisions made at PLANNED

**PO decision 1 — the limiter stores one entry per PLAYER, and `size()` counts
players.** AC-6 says the stored entry count equals the number of currently seated
players. Under a `(player, key)` table that is false for any player who used two
remotes, and the criterion would degrade into a statement about how many remotes
the test happened to exercise. Pinned in the Contract rather than amended into
the criterion, because the criterion is right and it is the *shape* that was
underdetermined. The `sizeCountsKeys` control exists to make the other shape
fail something.

**PO decision 2 — the limiter is constructed inside `guard`, not injected
through `GuardContext`.** The injected design is the one that lets a driver call
`forget` on a disconnect, and it was rejected here on evidence rather than taste:
every `GuardContext` in the tree is built by two **frozen** test helpers
(`NetContract.luau:133`, `PhaseContract.luau:117`), the tests are not
typechecked (the gate runs over `src` only), so a required field would surface as
a runtime nil across two merged stories' suites, and an optional field is a
signature change this story's scope denies. Recorded with its consequence:
`forget` and `size` have no production caller until the M3 driver exists, which
is the same "declared early, consumed later" move `NET-001` made with
`legalPhases`. Nothing is silently dead — AC-6 is a unit criterion on
`RateLimiter` and is phrased that way.

**PO decision 3 — `signal_rate_limit_seconds` does NOT go into
`src/shared/Tuning.luau`, and the reason was measured.** `Tuning`'s scope is
`tuning.md` §1 and §5 only (`Tuning.luau:14`), and `TuningSpec.luau:420-438`
fails any module key its parse of those two sections does not name. A §3 constant
added there reds `tuning_spec_test` and `tuning_controls_test`, both `ROUND-002`'s
and both frozen — a story returning to RED over a constant it never needed to
move. 1.5 reaches the suite through the remote's own declaration, and a single
provenance test with three vacuity assertions anchors it to `tuning.md` §3 so
that "settled: read it out" is a fact rather than a comment.

**PO decision 4 — `required_gates` stays `[]`.** `unit` is required and
`covers | unit | src/net/**` (added by `NET-001`) already brings both changed
source files under it, `src/net/RateLimiter.luau` included:
`bash scripts/classify.sh src/net/RateLimiter.luau` reports `source`. No optional
gate is the only one that exercises this story's artifact, which is the condition
that would force a `required_gates` entry before RED.

**PO decision 5 — the criteria were not touched at PLANNED, so this story carries
no `## Amendments` entry.** All six were already testable as written once the
Contract pinned the limiter's storage shape (decision 1) and where 1.5 comes from
(decision 3); neither needed a criterion to move. `check-boundaries.sh` compares
the criteria against the base branch, and they are identical to `main`'s.

### Epic done-when, checked against these criteria

`EPIC-02`'s done-when opens: *"Every remote has an adversarial test asserting
rejection of malformed, out-of-phase and **flooded** calls — B4's own definition
of done — and each of those tests asserts which check fired, not merely that
something was rejected."*

`NET-001` delivered malformed (`shape`, `range`), `NET-002` out-of-phase
(`phase`), and this story delivers **flooded** (`rate`) — the third of three, and
AC-1 asserts the reason by name rather than the fact of a rejection. With this
story the first clause of the epic's done-when is complete for the test remote
the three stories share.

**The gap that is not this story's to close, stated so it is not mistaken for
one:** there is still no *real* remote of the game to point those adversarial
tests at, because there is no driver — `architecture.md`'s composition root is
M3. Every one of the three stories says so in its own `## Out of scope`, and the
epic's remaining stories (`SEAT-003`, `TEL-002`, `TEL-003`) do not add one
either. So the done-when clause is satisfied *in the form the epic can be
satisfied in today*: the wrapper every remote will pass through has all five
adversarial tests, each naming its check. Nothing here needs the user to decide
anything, and this story is not asked to close it.


**Mutation the orchestrator should run at acceptance:**

1. Make `allow` record the timestamp on rejection as well as acceptance.
   Predicted: AC-3 goes red alone.
2. Key the limiter on `key` only, dropping `playerId`. Predicted: AC-2 goes red
   alone.
3. Change the interval comparison from `>=` to `>`. Predicted: AC-1's
   exactly-at-the-interval assertion goes red, the other two stay green.

Run mutation 1 first — it is the load-only bug and the assertion most likely to be
missing.

**Raise the `unit` floor** to the new real count.

### The orchestrator's RED acceptance, and the five escalations (2026-09-22)

RED reported five places where the Contract did not hold against the tree. Four
are corrections to the **Lead PO's own text** rather than to a criterion, and
`rules.md` still says a claim that the contract is wrong is reproduced before it
is accepted. Reproduced independently, without reusing RED's code:

**1. `recordsOnReject` cannot fail "AC-3 alone" — ACCEPTED, by arithmetic on the
criterion.** AC-1's own sequence is `t` accepted, `t + 1.4` rejected, `t + 1.5`
accepted. Under a limiter that records on rejection, the `t + 1.4` call stores
`t + 1.4`, so the `t + 1.5` call measures `0.1 < 1.5` and is refused — AC-1 goes
red with it. The Contract's control table and `## Notes` mutation 1 both said
"AC-3 alone" and both were wrong; the handoff's mutation table now predicts
**2**, and the orchestrator verifies it against the committed implementation
rather than against a stub.

**2. `rejectsEverything` cannot reach AC-4 at the limiter level — ACCEPTED, by
construction.** Under the pinned design a remote with no `rateLimit` gets no
limiter at all, so an `allow` that always returns false is never called on AC-4's
path. The control has to live in the wrapper's verdict, which is where RED put
it. My table's row was self-contradicting: it asked one defect to fail a
criterion its own design makes unreachable.

**3. A limiter keyed on `key` alone also reds AC-6 — ACCEPTED**, and it follows
from the shape I pinned myself: `size()` counts players and `forget` takes a
player, so dropping `playerId` from the key breaks both. The one-check catch is
the wrapper-level form.

**4. The provenance reader anchors on the row's FIRST CELL, not on the name
appearing — ACCEPTED, and reproduced with a grep of my own:**

    $ grep -rln 'signal_rate_limit_seconds' docs/
    docs/backlog/stories/NET-003.md
    docs/backlog/stories/TEL-003.md
    docs/wiki/game/mechanics.md
    docs/wiki/game/playtest.md
    docs/wiki/game/tuning.md

    $ grep -n 'signal_rate_limit_seconds' docs/wiki/game/tuning.md
    88:| `signal_rate_limit_seconds` | 1.5 | **derived** | two purposes at once: ...

The constant is named in four documents besides `tuning.md`, and inside
`tuning.md` a name can appear in another row's rationale cell — `signal_budget_
per_player` is named in this very row's rationale, which is the live example.
"Exactly one row **names** it" would have been a needle satisfied by prose.
First-cell anchoring is `rules.md`'s "anchor the match rather than letting it
float", applied to a table.

**5. My caller list was incomplete — ACCEPTED, and this one is a defect in the
PLANNED work, not a wording quibble.** I listed the guard sites in the two test
helpers and missed a third file:

    $ grep -rn '\.guard(' tests/net/*.luau
    tests/net/wrapper_test.luau:49:  local guarded = bundle.Wrapper.guard(definition, Contract.context({ "p1" }), function() end)

Read at `tests/net/wrapper_test.luau:41-51`: it builds `Contract.tokenRemote`,
which declares **no** `rateLimit`, and never calls the guarded function. So the
conclusion of `## Callers of changed signatures` survives — every guard site in
the tree takes AC-4's path — but it survived by luck of that site's shape rather
than by the list being complete. The list is corrected in the handoff's re-check
(19 sites, not 18). **The lesson is the one the section exists for:** a caller
list assembled from the files I expected to find callers in is not a caller list.

**The one judgement call RED raised, ANSWERED.** RED read AC-6's *"their entry is
removed"* as observable from outside — after `forget`, the player is allowed at
the very instant they were just refused — and amended the Contract to say so.
That reading is correct and it is the stronger of the two: a `size()` that drops
by one can be satisfied by a counter that decrements while the timestamp stays,
which is the same defect AC-6 exists to catch wearing a disguise. The criterion
is untouched; this is the Contract making it observable, which is exactly what
RED is allowed to do in place.

**What was verified rather than read.** `lune run test` → `348 passed, 7 failed`;
`grep -c '^  FAIL'` → 7, all in `rate_test.luau`, one per criterion. Six fail on
their own assertions against a wrapper that loads and ignores `rateLimit`
(`t + 1.4 -> ACCEPTED`, `context.clock.now() was not read`). **AC-6 fails
differently and deliberately** — `src/net/RateLimiter.luau did not load` — which
is the right failure for a unit criterion on a module that does not exist yet,
and it means **no assertion inside AC-6 has ever executed**. That is `rules.md`'s
import-failure case narrowed to one test, so AC-6's expectations are unverified
claims until GREEN, and GREEN is dispatched with confirming them as an explicit
obligation rather than a hope.

`git status --porcelain` before the RED commit listed six untracked test files,
`.claude/tests/project-counters.test.sh` and this story, and
`git diff --stat -- tests src` was empty: no frozen NET-001 or NET-002 file
moved, and no production file was written.

`bash scripts/gates.sh --fast` closing RED, run alone, read for admissibility
rather than for a pass:

    --- gate summary ---
    PASS         format (1s, observed 78)
    PASS         lint (1s, observed 78, floor 1)
    PASS         typecheck (4s, observed 15)
    FAIL         unit (31s, exit 1) -> .claude/state/gate-logs/unit.log
    UNCONFIGURED coverage
    PASS         build (1s, observed 44534)
    PASS         harness (19s, observed 40)

`unit.log` holds those seven `FAIL` lines and nothing else. `harness` is
`40 passed, 0 failed` on the committed tree, which is the counters precondition
clearing exactly as RED predicted. No gate failed on a timeout, a config error or
a lint rule a test file trips. `typecheck` reads 15 because the gates were run
alone; see the hazard note in `## Notes`.

### The orchestrator's GREEN acceptance, measured rather than reported (2026-09-22)

Run by the Lead PO against GREEN's working tree. A subagent's report is a claim,
and GREEN re-measuring its own work is GREEN checking its own work.

**1. The test freeze held.** `git status --porcelain` after GREEN returned:
`M docs/backlog/stories/NET-003.md`, `M src/net/Wrapper.luau`,
`?? src/net/RateLimiter.luau`. **No test file, and no fourth file.** The two
source paths are exactly the Contract's "What changes" rows for GREEN, and
`tests/helpers/NetStubs.luau`, `PhaseStubs.luau`, `NetContract.luau`,
`PhaseContract.luau` and every `tests/net/*_test.luau` are byte-for-byte as
merged.

**2. The suite is green.** `lune run test` → `355 passed, 0 failed`,
`grep -c '^  FAIL'` → 0, against the RED baseline of `348 passed, 7 failed`. The
seven that were red are the seven that now pass; 355 - 348 = 7.

**3. The mutation table is evidence, not a claim — two run independently of
GREEN's own.** Both predictions are for a **single** assertion, which is where a
vacuous test hides, and they land on different criteria.

*Mutation 3, the inclusive comparison (`## Notes` calls it the criterion's own
boundary):*

    $ bash scripts/mutate.sh src/net/RateLimiter.luau \
        's/now - lastAccepted < minIntervalSeconds/now - lastAccepted <= minIntervalSeconds/' \
        -- sh -c 'lune run test 2>&1 | grep -E "^  FAIL|passed,"'
    === mutate: src/net/RateLimiter.luau (1 line(s) changed) ===
      69 - 			if lastAccepted ~= nil and now - lastAccepted < minIntervalSeconds then
      69 + 			if lastAccepted ~= nil and now - lastAccepted <= minIntervalSeconds then

      FAIL  tests/net/rate_test.luau :: AC-1: a remote at minIntervalSeconds = 1.5 accepts a seated player at t, rejects the same player for rate at t + 1.4, and accepts them at exactly t + 1.5 - the comparison is inclusive
    354 passed, 1 failed

    === mutate: command exited 0; restored (verified byte-for-byte against
        .../mutations/src_net_RateLimiter.luau.20260922T171017Z.4023037.bak) ===
      69: 			if lastAccepted ~= nil and now - lastAccepted < minIntervalSeconds then

Predicted **1** test, AC-1 alone. Measured **1** test, AC-1 alone.

*Mutation 8, `forget` made a no-op — chosen second **because AC-6 is the test
whose assertions had never executed**. In RED it failed at
`src/net/RateLimiter.luau did not load`, so nothing inside it had run and every
expectation in it was an unverified claim. A mutation that reds it alone is what
turns those assertions from claims into measurements:*

    $ bash scripts/mutate.sh src/net/RateLimiter.luau \
        's/^\t\t\tentries\[playerId\] = nil$/\t\t\tlocal _ = playerId/' \
        -- sh -c 'lune run test 2>&1 | grep -E "^  FAIL|passed,"'
    === mutate: src/net/RateLimiter.luau (1 line(s) changed) ===
      84 - 			entries[playerId] = nil
      84 + 			local _ = playerId

      FAIL  tests/net/rate_test.luau :: AC-6: RateLimiter.new(1.5) holds one entry per PLAYER across keys, forget removes the whole entry so the player is new again, forgetting a stranger is a no-op, and size() equals the seated count through a round of eight joins and eight leaves
    354 passed, 1 failed

    === mutate: command exited 0; restored (verified byte-for-byte against
        .../mutations/src_net_RateLimiter.luau.20260922T171114Z.4025837.bak) ===
      84: 			entries[playerId] = nil

Predicted **1** test, AC-6 alone. Measured **1** test, AC-6 alone. Both restores
verified byte-for-byte by `mutate.sh` and logged; `git status` after them showed
only the three expected paths.

**4. RED's control values, confirmed against the shipped module.** RED measured
13 defect bundles against stubs, each held to all 28 checks. The claim about the
real code was that `src/net/` on arrival was `ignoresRateLimit` plus a missing
module, whose failing sets union to the seven red tests — and the converse now
holds: `rate_test.luau` is 8/8, `rate_controls_test.luau` 14/14 and
`rate_limit_provenance_test.luau` 6/6, so the real modules pass every check the
baseline stub passes. GREEN additionally re-measured five of the defects against
the **shipped** module rather than the stub and reports no divergence from RED's
numbers; the two mutations above are the orchestrator's own share of that, and
both matched.

**5. `.claude/tests/project-counters.test.sh`: literals moved 78/78/15 ->
79/79/16 by the Lead PO, in GREEN.** GREEN escalated this rather than moving it,
on the grounds that the file was not in its brief and that the file's own header
says to return to RED. The escalation was careful and the numbers it handed over
were right, but the return is not needed: `.claude/tests/**` classifies as
`harness`, which **GREEN may write** (`writes allowed: vendor,ignored,source,
config,manifest,docs,harness`), and `NET-001` moved these same literals in GREEN
for exactly this reason — the header records it: *"BEFORE THAT: NET-001 (GREEN),
which added the story's three production modules ... 63/63/12 -> 66/66/15"*. The
header's "go back to RED" is addressed to a **test** file count going stale, not
to a source file the current phase is entitled to add.

The numbers were read from the suite's own failure lines rather than counted:

    FAIL format reports 78 files on the unmodified tree
         expected count: 78
         actual count:   79
    FAIL typecheck reports 15 files on the unmodified tree
         expected count: 15
         actual count:   16
    FAIL narrowing the format target to src reports 15, not 78
         expected count: 15
         actual count:   16
    project-counters: 28 passed, 12 failed

`NARROW_TYPECHECK` (`src/shared` alone) did **not** fail and stays at 7, which is
the measurement that it did not move: `RateLimiter.luau` is under `src/net/`.
After the move, with the source files still uncommitted:

    $ bash .claude/tests/project-counters.test.sh
        FAIL the working tree carries no stray .luau files, so the baselines mean what they say
    project-counters: 39 passed, 1 failed

The one remaining failure is the stray-file precondition listing GREEN's own two
uncommitted source paths; it clears on the commit, as it did in RED.

**6. A harness defect GREEN found, reproduced by the orchestrator and filed
elsewhere.** `scripts/mutate.sh` piped into `head` can be killed by SIGPIPE
*after* its restore `cp` and *before* its cleanup and log append, leaving a
`.bak`/`.new` pair and no log line — which `rules.md` and
`.claude/state/README.md` both read as **"a restore failed"**. It had not.
Verified independently rather than taken on trust:

    $ cmp .claude/state/mutations/src_net_Wrapper.luau.20260922T165540Z.3977788.bak src/net/Wrapper.luau
    (no output: identical)
    $ tail -4 .claude/state/mutations/log
    (no entry for 20260922T165540Z; the two runs either side are logged
     `restored (verified)`)

So the tree was correct and the signal was false. The stale pair was removed once
this paragraph recorded it, so that `.claude/state/mutations/` stops asserting a
failure that did not happen — the directory is machine-local and gitignored, and
nothing committed changes. **This is not NET-003's to fix**: it is harness work,
it wants its own `HARNESS-*` story with a test in `.claude/tests/mutate.test.sh`
that pipes the script into an early-closing reader and watches it fail first, and
it has been handed to the user as a separate task rather than absorbed here.

### PR #24's CI timings, read rather than assumed (REVIEW, 2026-09-22)

https://github.com/ryanczhang7/first-roblox/pull/24 — both required checks green
on the first run. The *timings* are read and not just the verdict, because a pass
within 10 % of a limit is a pending failure and a story has been sent back from
REVIEW by exactly that.

| Job | Run | Wall | Verdict |
|---|---|---|---|
| `gates` | [35761654117](https://github.com/ryanczhang7/first-roblox/actions/runs/35761654117) | **2m24s** | success |
| `boundaries` | [35761653923](https://github.com/ryanczhang7/first-roblox/actions/runs/35761653923) | **11s** | success |

Step by step, from the run's own job API rather than from the summary:

    Install the pinned toolchain:   6s
    Harness self-test:            102s   (every .claude/tests suite, 0 failed)
    Show configured gates:          3s
    Audit the gate manifest:        4s
    Run gates:                     21s

and the gate summary CI produced, gate for gate identical to the one recorded
locally in `## Gate results`:

    PASS         format (0s, observed 79)
    PASS         lint (0s, observed 79, floor 1)
    PASS         typecheck (3s, observed 16)
    PASS         unit (2s, observed 355, floor 355)
    PASS         build (0s, observed 47816)
    PASS         harness (13s, observed 40)
    All required gates passed (6 ran, 3 unconfigured, 0 known).

**Nothing is near a limit.** No gate in `project.conf` carries a timeout and the
runner has none per test; the longest CI step is the harness self-test at 102 s
inside a job that took under two and a half minutes. It grew from NET-002's 75 s
with this story's six new test files and one new module, which is the number to
watch across the next few stories rather than a problem now.

**The `unit` gate reads 2 s on CI against 27-47 s locally**, and this story is
the one where that gap could have meant something, because it is the story about
time. It does not: the suite does not sleep, so the gap is Windows subshell and
file-IO cost, the same factor `NET-001` and `NET-002` measured. The evidence that
the two machines ran the same work is that `observed 355` and `project-counters:
40 passed, 0 failed` are identical on both, and the floor of 355 is live on both
— CI compared against it and passed rather than skipping the comparison.

**`gates.sh --list` took 3 s here** (`Show configured gates`) against several
minutes on this Windows machine — the fork-cost note `NET-002` recorded is
unchanged and is not this story's.

## Test plan

Six new test files and **no change to any NET-001 or NET-002 file**, following
the pattern those stories settled: checks in a `*Contract.luau` helper
parameterised over the subject bundle, defective subjects in a `*Stubs.luau`
factory layered over the previous story's correct bundle, the real suite
against `src/net/`, and a controls suite that needs nothing from GREEN and
therefore RUNS in RED. The `tuning.md` §3 provenance guard stands apart with
its own reader.

| File | Covers |
|---|---|
| `tests/helpers/RateContract.luau` | 8 checks over a `{ Schema, Remotes, Wrapper, RateLimiter }` bundle: AC-1 to AC-6 and the Contract's two promises about a rate rejection (`detail` names the remote and the interval with no prefix; `context.clock.now()` is read only by calls that reach check 5, and the value compared is that call's). `world(seated, phase, start)` builds a `GuardContext` whose clock is `Clock.manual` with every `now()` read counted; `MIN_INTERVAL_SECONDS = 1.5` is the one place the settled number is spelled. |
| `tests/helpers/RateStubs.luau` | a factory over one-field defect tables, layered over `PhaseStubs.correct()` (itself over `NetStubs.correct()`): a correct rate-limiting baseline, a stub `RateLimiter`, and 13 wrong bundles including the ones the criteria and the Contract name (`sharedTimestamp`, `recordsOnReject`, `strictlyGreater`, `limitsWithoutRateLimit`, `rateBeforePhase`, `neverForgets`, `sizeCountsKeys`, `rejectsEverything`) |
| `tests/helpers/RateLimitSpec.luau` | the provenance reader: every table row of `tuning.md` whose first cell is `` `signal_rate_limit_seconds` ``, its section, its value cell; `violations` with the three vacuity assertions built in, the first of which is "exactly one row" |
| `tests/net/rate_test.luau` | 8 tests against the real `src/net/Wrapper.luau` and the (absent) `src/net/RateLimiter.luau`. **7 red in RED**, on their own assertions; AC-4 green on arrival and earned by the `limitsWithoutRateLimit` control and by a `mutate.sh` probe of the real wrapper in `## Regressions`. |
| `tests/net/rate_controls_test.luau` | 14 tests: a baseline that passes all 8 rate checks **and** NET-002's 8 phase checks **and** NET-001's 12 wrapper/registry checks (28), and 13 controls each asserted to FAIL the checks it is built for AND PASS the rest, with the measured counts and message fragments pinned. **All green in RED.** |
| `tests/net/rate_limit_provenance_test.luau` | 6 tests: the guard over the real `tuning.md`, and five in-memory controls - no row (vacuity 1), two rows (vacuity 1), a non-numeric value cell (vacuity 2), a different value (the comparison), and the real row shape beside a longer name that must not match. **All green in RED**; the guard is earned by a `mutate.sh` probe of the real document in `## Regressions`. |

Per test, what it asserts and which AC it covers:

| Test (`rate_test.luau`) | Asserts | AC |
|---|---|---|
| `AC-1: a remote at minIntervalSeconds = 1.5 accepts a seated player at t, rejects the same player for rate at t + 1.4, and accepts them at exactly t + 1.5 - the comparison is inclusive` | one guard, clock SET (not advanced) to `t`, `t + 1.4`, `t + 1.5` with `t = 10`: `nil`, `{ reason = "rate", detail = <non-empty> }`, `nil`; handler ran exactly 2. The criterion's three calls and nothing else, so `>` reds one line here and nothing elsewhere | AC-1 (settled: 1.5 read out of `tuning.md` §3 through the declaration) |
| `AC-2: while player A is rate-limited, player B's call at the same instant is accepted, and each is then held to their own interval - one shared timestamp fails here` | one guard, two seated players: `A@t -> nil`, `A@t+0.5 -> rate`, `B@t+0.5 -> nil`, `B@t+0.6 -> rate`, `A@t+0.6 -> rate`; handler ran 2, only for A and B | AC-2 |
| `AC-3: the interval is measured from the last ACCEPTED call - a player rejected at t + 0.5, 1.0 and 1.4 is accepted at t + 1.6, and 320 calls at 10 ms after an acceptance are served exactly twice, not locked out` | `t -> nil`, `t+0.5, t+1.0, t+1.4 -> rate`, `t+1.6 -> nil`, handler 2; then a fresh guard/player accepted at `t`, and 320 calls at `t + i/100`: every non-accepted call is `rate`, exactly **2** accepted (grid 150/300 under `>=`, 151/302 under `>` - blind to the boundary by design), load handler ran 3 | AC-3 |
| `AC-4: a remote declared with no rateLimit accepts fifty calls at one instant, runs the handler fifty times, and never reads context.clock.now() - not during guard() and not during any call` | `definition.rateLimit == nil`; 50 calls at a frozen clock all `nil`; handler 50; clock reads counted from BEFORE `guard()` are **0** | AC-4 (incl. structural form) |
| `AC-5: calls rejected for identity, shape, range or phase read the clock zero times and do not advance the player's timestamp - ...` | accepted at `t`; at `t+1.0`: `p2 -> identity`, `{} -> shape`, `{ token = 99 } -> range`, `VALID in Lobby -> phase`, each with **0** clock reads; `VALID@t+2.0 -> nil`; `VALID in Lobby @t+4.0 -> phase` (0 reads); `VALID@t+5.0 -> nil`; `VALID@t+5.5 -> rate` (the positive control inside the check); handler 3 | AC-5 |
| `AC-6: RateLimiter.new(1.5) holds one entry per PLAYER across keys, forget removes the whole entry so the player is new again, forgetting a stranger is a no-op, and size() equals the seated count through a round of eight joins and eight leaves` | `RateLimiter` loaded and `new` is a function; `limiter:allow/forget/size` are methods (colon call); `size` 0 -> 1 (p1 on remoteA) -> 1 (p1 refused at 0.5) -> **1** (p1 on remoteB, allowed) -> 2 (p2); `forget(p1)` -> 1 and `allow(p1, remoteA, 0.5)` is **true** again; both forgotten -> 0; `forget("never-seen")` no raise, 0; eight players each allowed on two keys and refused on a third call, `size == #seated` after every join and every leave down to 0 | AC-6 (unit; the Contract's storage shape) |
| `Contract: a rate rejection's detail is non-empty, names the remote and carries 1.5, and does not lead with a rate: prefix` | first call `nil`; second at the same instant `rate`; `detail` non-empty, `string.match(detail, "^rate:") == nil`, contains `definition.name` and `"1.5"` as plain substrings | Contract (`detail` block) |
| `Contract: context.clock.now() is read zero times on a call identity, shape, range or phase rejected, at least once on a call that reaches check 5, and the value compared is the one that call returned` | guard built at `t`; clock set to `t+40`: identity/shape/range/phase rejections with **0** reads each; `VALID -> nil` with **>= 1** read; `t+40.5 -> rate` with >= 1 read; `t+42.5 -> nil` (a clock captured at `guard()` sees 0 s elapsed forever and refuses this); handler 2 | Contract (read-count block) |

| Test (`rate_limit_provenance_test.luau`) | Asserts | AC |
|---|---|---|
| `AC-1 provenance: docs/wiki/game/tuning.md names signal_rate_limit_seconds in exactly one table row, its value cell is a number, and that number equals the 1.5 the rate suite declares` | the file is non-empty; **exactly 1** row (first-cell match); `tonumber(valueCell)` is a number, asserted before comparing; `Spec.violations` empty against `RateContract.MIN_INTERVAL_SECONDS` | AC-1 (provenance; oracle-free reader) |
| `provenance control: a document in which the name appears only in prose and in another row's rationale has no row - ...` | 0 rows; exactly 1 violation naming `the extraction matched nothing` and the first-cell rule | vacuity 1 |
| `provenance control: two rows naming the constant are reported by count and line, even when both carry the right value` | 2 rows; exactly 1 violation: `2 table rows name ... (lines 6, 7)` | vacuity 1 |
| `provenance control: a value cell that is not a bare number - 1.5 s - is reported as not a number, not silently read as 1.5` | exactly 1 violation naming `"1.5 s", which is not a number` and `§3` | vacuity 2 |
| `provenance control: a document value of 2 is reported against the suite's 1.5, naming both numbers and the document as the oracle` | exactly 1 violation: `is 2 in the document; the suite declares 1.5` | the comparison |
| `provenance control: the real row shape yields the value 1.5 and no violation, and a row whose first cell is a longer name containing this one is not a match` | 1 row at line 7, section 3, value 1.5; `signal_rate_limit_seconds_v2` not matched; 0 violations | reader shape |

The controls suite (`rate_controls_test.luau`, 14 tests) is enumerated in
`## Handoff` with its measured numbers.

**Deliberately not written.** No test that the key passed to `allow` is
`definition.name`: the wrapper's limiter is internal and no test can observe
its key, so the Contract's sentence stays a Contract sentence. No test of a
per-server or global limit, of banning or escalation, of burst tolerance, of
the signal budget, or of client-side throttling (`## Out of scope`). No test
of `allow` with `now` moving backwards, of a negative or zero
`minIntervalSeconds`, or of `RateLimiter.new` validating its argument: none
is a criterion, and `Clock.advance` already refuses negative deltas. No test
that a declaring remote does NOT read the clock during `guard()` construction:
the Contract leaves that free, and only AC-4's non-declaring remote pins zero
reads ever. No test of two remotes through one wrapper sharing a limiter: the
Contract builds one limiter per `guard()` call, so a player's limit on one
remote is independent of another by construction, and AC-6 pins the per-key
independence at the limiter level instead. No wall-clock time anywhere: every
timed sequence is `clock:set`.

## Handoff: RED -> GREEN

**Dispatch.** RED ran on **claude-fable-5-1** (`fable`), which is what
`## Model guidance` planned. The brief announced no override; whether the
agent file's `model:` or a per-dispatch setting resolved it is not visible from
inside the dispatch, and the orchestrator records which under `## Model
guidance`.

### Files written

| File | What it is |
|---|---|
| `tests/helpers/RateContract.luau` | the 8 checks over any `{ Schema, Remotes, Wrapper, RateLimiter }` bundle; `world()`, `remote()`, `MIN_INTERVAL_SECONDS` |
| `tests/helpers/RateStubs.luau` | factory over one-field defect tables, layered over `PhaseStubs.correct()`: the baseline, a stub limiter, 13 wrong bundles |
| `tests/helpers/RateLimitSpec.luau` | the `tuning.md` row reader with the vacuity assertions built in |
| `tests/net/rate_test.luau` | 8 tests against the real modules. **7 red, 1 green** (AC-4) |
| `tests/net/rate_controls_test.luau` | 14 control tests. **All green in RED** |
| `tests/net/rate_limit_provenance_test.luau` | the guard and 5 controls, 6 tests. **All green in RED**, the guard earned in `## Regressions` |
| `.claude/tests/project-counters.test.sh` | the file-count literals moved for six new `.luau` files; see `## Regressions` |
| `docs/backlog/stories/NET-003.md` | `## Contract` (three in-place amendments, each marked), `## Test plan`, this section, `## Regressions` |

**No NET-001 or NET-002 file was touched**: `git diff --stat -- tests src` is
empty - `NetContract.luau`, `NetStubs.luau`, `PhaseContract.luau`,
`PhaseStubs.luau` and every existing `tests/net/*_test.luau` are byte-for-byte
as merged. No production file was written. No dependency was added.
`## Deferred verifications` is untouched: nothing is deferred, every control
ran here. Every touched path was put to `bash scripts/classify.sh`
individually: six `test`, one `harness`, one `docs`.

### The command, and the verbatim failure

    lune run test

Seven failures, every one an assertion in `rate_test.luau` against the real
`src/net/` - `Wrapper.luau` loads, because NET-001 built it, and ignores the
rate limit, because nothing has read `rateLimit` yet; `RateLimiter.luau` does
not exist, and AC-6 says so by name - and every other file green, NET-001's
and NET-002's 327 included (`327 tests` on arrival; `348 passed, 7 failed`
now: 28 new cases, 21 green, 7 red). Wall time 23.9 s on this machine.

      FAIL  tests/net/rate_test.luau :: AC-1: a remote at minIntervalSeconds = 1.5 accepts a seated player at t, rejects the same player for rate at t + 1.4, and accepts them at exactly t + 1.5 - the comparison is inclusive
            C:\Users\ryanc\Projects\first-roblox\tests\helpers\RateContract:277: AC-1: 2 violation(s) across 3 calls at minIntervalSeconds = 1.5 - the comparison is now - lastAccepted >= minIntervalSeconds, inclusive:
              AC-1: the same player at t + 1.4 -> ACCEPTED (returned nil); the handler was reachable
              AC-1: the handler ran 3 time(s) over 3 calls; 2 were outside the interval
      FAIL  tests/net/rate_test.luau :: AC-2: while player A is rate-limited, player B's call at the same instant is accepted, and each is then held to their own interval - one shared timestamp fails here
            C:\Users\ryanc\Projects\first-roblox\tests\helpers\RateContract:331: AC-2: 4 violation(s) across 5 calls by two players - an implementation with one shared last-accepted timestamp fails here:
              AC-2: A at t + 0.5 (inside A's interval) -> ACCEPTED (returned nil); the handler was reachable
              AC-2: B at t + 0.6 (inside B's own interval) -> ACCEPTED (returned nil); the handler was reachable
              AC-2: A at t + 0.6 (still inside A's) -> ACCEPTED (returned nil); the handler was reachable
              AC-2: the handler ran 5 time(s) over 5 calls; 2 were accepted
      FAIL  tests/net/rate_test.luau :: AC-3: the interval is measured from the last ACCEPTED call - a player rejected at t + 0.5, 1.0 and 1.4 is accepted at t + 1.6, and 320 calls at 10 ms after an acceptance are served exactly twice, not locked out
            C:\Users\ryanc\Projects\first-roblox\tests\helpers\RateContract:427: AC-3: 5 violation(s) across 326 calls - an implementation that updates the timestamp on rejection fails here:
              AC-3: rejected at t + 0.5 -> ACCEPTED (returned nil); the handler was reachable
              AC-3: rejected at t + 1.0 -> ACCEPTED (returned nil); the handler was reachable
              AC-3: rejected at t + 1.4 -> ACCEPTED (returned nil); the handler was reachable
              AC-3: the handler ran 5 time(s) over 5 calls; 2 were outside the interval
              AC-3: over 320 calls at 10 ms (3.2 s of 100 Hz) after an acceptance, the player was served 320 time(s); a limit of one call per 1.5 s serves them 2 times - an implementation that records the timestamp on rejection locks them out forever
      FAIL  tests/net/rate_test.luau :: AC-5: calls rejected for identity, shape, range or phase read the clock zero times and do not advance the player's timestamp - a valid call 2.0 s later is accepted, an out-of-phase call outside the interval advances nothing, and a call 0.5 s after an acceptance is rate
            C:\Users\ryanc\Projects\first-roblox\tests\helpers\RateContract:631: AC-5: 2 violation(s) across 9 calls - a limiter consulted before identity, shape, range or phase, or advanced by a call they rejected, fails here:
              AC-5: valid at t + 5.5 -> rate (the limiter is measured from t + 5.0) -> ACCEPTED (returned nil); the handler was reachable
              AC-5: the handler ran 4 time(s) over 9 calls; 3 were accepted
      FAIL  tests/net/rate_test.luau :: AC-6: RateLimiter.new(1.5) holds one entry per PLAYER across keys, forget removes the whole entry so the player is new again, forgetting a stranger is a no-op, and size() equals the seated count through a round of eight joins and eight leaves
            C:\Users\ryanc\Projects\first-roblox\tests\helpers\RateContract:652: AC-6: src/net/RateLimiter.luau did not load - the limiter is a new module and AC-6 is a unit criterion on it
      FAIL  tests/net/rate_test.luau :: Contract: a rate rejection's detail is non-empty, names the remote and carries 1.5, and does not lead with a rate: prefix
            C:\Users\ryanc\Projects\first-roblox\tests\helpers\RateContract:797: detail: the second call at the same instant -> ACCEPTED (returned nil); the handler was reachable
      FAIL  tests/net/rate_test.luau :: Contract: context.clock.now() is read zero times on a call identity, shape, range or phase rejected, at least once on a call that reaches check 5, and the value compared is the one that call returned
            C:\Users\ryanc\Projects\first-roblox\tests\helpers\RateContract:951: clock: 5 violation(s) across 7 calls:
              clock: a valid payload in phase at t + 40, the guard having been built at t (accepted) -> context.clock.now() was not read during the call, yet the call was judged on its timing
              clock: the same player at t + 40.5 (rate) -> ACCEPTED (returned nil); the handler was reachable
              clock: the same player at t + 40.5 (rate) -> context.clock.now() was not read during the call, yet the call was judged on its timing
              clock: the same player at t + 42.5, 2.5 s after the acceptance (accepted - the clock of the moment, not the clock at guard()) -> context.clock.now() was not read during the call, yet the call was judged on its timing
              clock: the handler ran 3 time(s) over 7 calls; 2 were accepted
    348 passed, 7 failed

(Verbatim from the runner, re-indented only, as NET-002 did.)

**Why this is the right failure.** Six of the seven fail on the assertion the
criterion is about - an inside-the-interval call `ACCEPTED (returned nil)`, a
100 Hz caller `served 320 time(s)`, `context.clock.now() was not read` -
against a module that loaded. The seventh, AC-6, fails on
`src/net/RateLimiter.luau did not load`, and that IS its right failure: the
module is the first row of the Contract's "What changes", AC-6 is a unit
criterion on it and can fail no other way until it exists, and the wrapper
tests were deliberately kept from depending on it (`net()` carries it as nil)
so that they fail on their own terms rather than on its absence. Two things
follow. First, the real wrapper today is exactly the `ignoresRateLimit` stub
defect: the six wrapper tests it fails are the six that control fails
(`6 of 28`), and the seventh is the missing module - the controls suite and
the real suite agree about the world. Second, **AC-4 is green on arrival and
is not earned by this run**: the current wrapper never reads the clock and
accepts every seated, valid, in-phase call. It is earned twice - by the
`limitsWithoutRateLimit` control (fails AC-4 with `49 of 50 calls were not
accepted` and `context.clock.now() was read 50 time(s)`, the only one of this
story's checks it fails) and by a `mutate.sh` probe of the real
`Wrapper.luau` in `## Regressions`. AC-1's **first call** is likewise green
for free and is earned by `rejectsEverything`, which fails AC-1 with
`the first call, at t -> REJECTED: reason = "rate"`.

`bash scripts/gates.sh --fast`, read for the shape of the failure rather than
for a pass:

    --- gate summary ---
    PASS         format (1s, observed 78)
    PASS         lint (1s, observed 78, floor 1)
    PASS         typecheck (4s, observed 15)
    FAIL         unit (53s, exit 1) -> .claude/state/gate-logs/unit.log
    UNCONFIGURED coverage
    PASS         build (1s, observed 44534)
    FAIL         harness (29s, exit 1) -> .claude/state/gate-logs/harness.log
    2 required gate(s) failed.

**format, lint, typecheck and build PASS over the six new files** (78 `.luau`
observed, up from 72; `selene`: 0 errors, 0 warnings; `stylua --check`:
clean); `unit` FAIL with exactly the seven failures above and nothing else in
`unit.log`; `harness` FAIL on `project-counters` only - the file-count literals
were stale for six new `.luau` files and the working tree carried them
untracked, both of which `## Regressions` records and the first of which it
fixes. No gate failed on a timeout, a config error or a lint rule a test file
trips. `typecheck ... observed 15` is the correct count this time: the suite
was not run alongside the gates. `coverage` is unconfigured. `--fast` is a
read, not a record.

### The export shape the tests already pin

Nothing below is a suggestion. Each name is already called by a test through
the checks in `RateContract.luau`, so a wrong guess is a red test rather than
a debate. The tests require the modules from `tests/net/` as
`require("../../src/net/Schema")`, `.../Remotes`, `.../Wrapper` and
`.../RateLimiter`, and **no signature changes**: `Wrapper.guard`,
`GuardContext`, `Rejection`, `Remotes.define` and `Remotes.all` keep their
shapes.

    src/net/RateLimiter.luau  -  a NEW module returning a table with `new`

      RateLimiter.new(minIntervalSeconds: number) -> limiter
        called as RateLimiter.new(1.5); returns a TABLE whose `allow`,
        `forget` and `size` are functions called with COLON syntax
        (self first), exactly as the Contract's type says:

      limiter:allow(playerId, key: string, now: number) -> boolean
        - true for a (player, key) the limiter has never seen, at any `now`
        - otherwise true iff now - lastAccepted >= minIntervalSeconds
          (INCLUSIVE: allow(p, k, 10) then allow(p, k, 11.5) is true;
          allow(p, k, 11.4) is false)
        - RECORDS `now` for that (player, key) ONLY when it returns true
          (AC-3: after true@10, false@10.5, false@11.0, false@11.4 the call
          at 11.6 is true)
        - one player's keys are independent: true@0 on "remoteA", false@0.5
          on "remoteA", true@0.5 on "remoteB"
        - never reads a clock; ROUND-001's source guard covers the file
      limiter:forget(playerId) -> ()
        - drops the player's whole entry: their next allow at ANY `now`,
          including the instant they were just refused, is true
        - for a player never seen: no raise, size() unchanged
      limiter:size() -> number
        - the number of PLAYERS with an entry, not (player, key) pairs: one
          player who used two keys counts 1; eight players on two keys each
          count 8; after forget it falls by exactly one per leaver, to 0

    src/net/Wrapper.luau  -  Wrapper.guard(definition, context, handler)

      The guarded function, per call, after identity / shape / range / phase
      have all passed and BEFORE the handler:

        - if definition.rateLimit == nil: does NOT read context.clock.now()
          - not during guard(), not during any call - and accepts every
          call (AC-4: 50 calls at one frozen instant, 50 handler runs)
        - otherwise reads context.clock.now() AT LEAST ONCE during that
          call (the test counts reads per call and requires >= 1 on every
          call that reaches check 5, accepted or rate-rejected) and compares
          the value that call returned, not one read at guard() (the guard
          is built at t and first called at t + 40; a call at t + 42.5 must
          be accepted)
        - does NOT read context.clock.now() on a call identity, shape,
          range or phase has already rejected (0 reads required) and does
          not advance any timestamp on such a call (AC-5: an out-of-phase
          call at t + 4.0 after an acceptance at t + 2.0 leaves a valid
          call at t + 5.0 accepted)
        - the limiter's state persists across calls to ONE guarded function
          (one guard, calls at t, t + 1.4, t + 1.5) and is per player (A
          limited at t + 0.5 while B is accepted at t + 0.5)
        - accepts when now - lastAccepted >= minIntervalSeconds, INCLUSIVE:
          accepted at t, rejected at t + 1.4, accepted at exactly t + 1.5
        - otherwise returns { reason = "rate", detail = <string> } and the
          handler does not run, where detail:
            * is non-empty
            * does NOT match "^rate:" (no kind prefix; reason carries it)
            * CONTAINS definition.name as a plain substring
            * CONTAINS "1.5" as a plain substring (tostring(1.5) of the
              declared minIntervalSeconds)
          e.g. the stub's `"NetTest_ac1_7" is limited to one call per 1.5 s`
          satisfies it. Wording otherwise free.

**Not constrained, so it stays yours:** the exact wording of the rate
`detail` beyond the substrings above, including whether it says how long the
caller waited; the key passed to `allow` (the Contract says
`definition.name`; no test can see it through the wrapper); whether a
DECLARING remote's guard reads the clock during `guard()` construction (only
the non-declaring remote pins zero reads ever); whether the clock is read once
or more per call; whether `allow` is consulted on a rate-rejected call or the
wrapper computes the comparison some other way, so long as the clock is read
and the timestamp is not advanced; what `allow` does with `now` moving
backwards or a non-positive interval; whether `RateLimiter.new` validates its
argument; the internal table shape beyond what `size()` and `forget` observe;
how `>=` is spelled.

### The negative controls, with their measured values

Every control below EXECUTED in RED, in `tests/net/rate_controls_test.luau`,
against `RateStubs.luau` bundles layered over NET-002's correct stub; the
numbers were measured first with a scratch script outside the runner (one
`pcall` per check per bundle, output kept in the session's scratchpad), then
pinned as needles, then confirmed by the suite (`348 passed`). Each control is
held to all 28 checks - the 8 rate checks, NET-002's 8 phase checks and
NET-001's 12 wrapper/registry checks - so "fails N of 28" is exact, and
"passes NET-001 and NET-002" is asserted rather than assumed. What they are
NOT is measurements against the shipped module: **confirming that the real
`src/net/` passes the same 8 checks the baseline stub passes is GREEN's job**,
and `rate_test.luau` going green is how it is done.

| Control (one defect) | Named by | Fails (of 28) | The number the check measured |
|---|---|---|---|
| baseline (no defect) | - | **0** | passes all 8 rate, 8 phase and 12 NET-001 checks |
| `sharedTimestamp` - one id passed to `allow` for everybody | **AC-2**, mutation 2 | **1**: AC-2. **Passes AC-1** (the criterion's claim, word for word) and the other 26 | AC-2: `2 violation(s) across 5 calls by two players` - `B at t + 0.5, the same instant A is rate-limited -> REJECTED: reason = "rate"`, `the handler ran 1 time(s) over 5 calls; 2 were accepted`; A's first call still accepted, no inside-interval call accepted |
| `recordsOnReject` - `allow` records on false too | **AC-3**, mutation 1 | **2**: AC-3 and AC-1 (see the Contract amendment). **Passes AC-2, AC-4, AC-5, AC-6, detail, clock and all 20 earlier** | AC-3: `3 violation(s) across 326 calls` - `t + 1.6 ... -> REJECTED: reason = "rate"`, `the player was served 0 time(s); a limit of one call per 1.5 s serves them 2 times`; AC-1: `the same player at t + 1.5, exactly the interval -> REJECTED` |
| `strictlyGreater` - `>` for `>=` | **AC-1** (boundary), mutation 3 | **1**: AC-1, on ONE call | AC-1: `2 violation(s) across 3 calls` - `the same player at t + 1.5, exactly the interval -> REJECTED: reason = "rate"`, `the handler ran 1 time(s) over 3 calls; 2 were outside the interval`; first call accepted, t + 1.4 still refused |
| `ignoresRateLimit` - no check (the real wrapper today) | - | **6**: AC-1, AC-2, AC-3, AC-5, detail, clock. **Passes AC-4, AC-6 and all 20 earlier** | AC-1: `the same player at t + 1.4 -> ACCEPTED`, `the handler ran 3 time(s)`; AC-3: `the player was served 320 time(s)`; AC-5: `2 violation(s) across 9 calls` - only `valid at t + 5.5 -> rate ... -> ACCEPTED` and the handler count: every earlier reason is still right |
| `limitsWithoutRateLimit` - limits a remote declaring none | **AC-4** | **4**: AC-4, NET-002 AC-6, NET-001 AC-1, NET-001 AC-5. **Among this story's checks, AC-4 alone** | AC-4: `3 violation(s)` - `49 of 50 calls were not accepted; the first: AC-4: call 2 at the same instant -> REJECTED: reason = "rate"`, `the handler ran 1 time(s) over 50 calls`, `context.clock.now() was read 50 time(s) across guard() and 50 calls`; NET-001 AC-1: `a second valid call was not accepted: reason = "rate"` - the three earlier checks that call one guard repeatedly at a frozen clock, exactly as `## Callers of changed signatures` predicted |
| `rateBeforePhase` - check 5 ahead of check 4 | **AC-5** | **2**: AC-5, clock. **Passes AC-1 to AC-4, AC-6, detail, all 20 earlier**; identity, shape and range NOT displaced | AC-5: `5 violation(s) across 9 calls` - `out of phase (Lobby) at t + 1.0 -> phase -> rejected for "rate", expected "phase"`, `... at t + 4.0, outside the interval -> phase -> context.clock.now() was read 1 time(s)`, `valid at t + 5.0 -> ... -> REJECTED: reason = "rate"`, `valid at t + 5.5 -> rate ... -> ACCEPTED`; clock: `3 violation(s) across 7 calls` - `a valid payload out of phase (phase) -> context.clock.now() was read 1 time(s)` |
| `rejectsEverything` - the wrapper refuses every call reaching check 5 | positive control for **AC-1**'s first call and **AC-4** | **15**: AC-1, AC-2, AC-3, AC-4, AC-5, detail, clock, NET-002 AC-1/AC-3/AC-4/AC-6, NET-001 AC-1/AC-5/short-circuit/kind mapping. **Passes AC-6 and every check that only refuses** | AC-1: `3 violation(s) across 3 calls` - `the first call, at t -> REJECTED: reason = "rate"`, `the handler ran 0 time(s) over 3 calls`; AC-4: `50 of 50 calls were not accepted`, and NO clock-read line (no limiter for that remote; the verdict is the defect) |
| `capturesClockAtGuard` - `now()` read once at `guard()` | the read-count block | **4**: AC-1, AC-3, AC-5, clock. **Passes AC-2** (never moves past the interval), AC-4, AC-6, detail, all 20 earlier | clock: `5 violation(s) across 7 calls` - `the guard having been built at t (accepted) -> context.clock.now() was not read during the call`, `2.5 s after the acceptance ... -> REJECTED: reason = "rate"` |
| `prefixedDetail` - `"rate: ..."` | Contract | **1**: detail | `failed in 1 way(s)`: `detail leads with "rate:"` |
| `detailOmitsInterval` | Contract | **1**: detail | `1 way(s)`: `detail does not carry the required interval 1.5` |
| `detailOmitsName` | Contract | **1**: detail | `1 way(s)`: `detail does not name the remote` |
| `neverForgets` - `forget` a no-op | **AC-6** | **1**: AC-6 | `20 violation(s)` - `size() is 2 after forget(p1); expected 1`, `allow("p1", "remoteA", 0.5) returned false (p1 is new again after forget ...); expected true`, `size() is 10 after player1 left, with 0 seated; expected 0` |
| `sizeCountsKeys` - `size()` counts pairs | **AC-6** (the pinned shape) | **1**: AC-6 | `17 violation(s)` - `size() is 2 after p1 used a second remote - one entry per PLAYER, not per (player, key); expected 1`, `size() is 16 with 8 seated; expected 8`; no `returned false` line - every allow answer is right, only the count is wrong |

Three things the table shows that "controls exist" would not: the three
named single-defect controls fail **disjoint** criteria among the wrapper
tests (AC-2, AC-1, and AC-3+AC-1) and the two AC-6 controls are
single-criterion catches; the real wrapper on arrival is precisely
`ignoresRateLimit`, whose six failures plus the absent module are the seven
red tests; and the `recordsOnReject` control fails one thing beyond AC-3 -
AC-1's own boundary call - which is a fact about that defect and about the
criterion's text, recorded (and amended into the Contract) rather than tuned
away.

**The provenance controls** ran in RED as well, all green, in
`rate_limit_provenance_test.luau`; their expected values are in `## Test
plan`. The guard itself was probed against the **real document** through
`scripts/mutate.sh` - pasted in `## Regressions` - and goes red naming the
line, the section and both numbers.

### Mutation table for acceptance

Predictions against the **committed implementation**, once GREEN is done; the
orchestrator verifies two. Test names are `rate_test.luau :: <name>` unless
said otherwise; the controls suite and the provenance suite are unaffected by
any of these because neither loads `src/net/`.

| # | Mutation (`bash scripts/mutate.sh FILE 'EXPR' -- lune run test`) | Predicted red | Count |
|---|---|---|---|
| 1 | `src/net/RateLimiter.luau`: record the timestamp on rejection as well as acceptance | **AC-3** (`t + 1.6 ... -> REJECTED`, `served 0 time(s)`) **and AC-1** (`t + 1.5, exactly the interval -> REJECTED`). `## Notes` predicts "AC-3 alone"; that cannot hold, because AC-1's own calls at t + 1.4 and t + 1.5 are 0.1 s apart - measured on the stub, `2 of 28` | **2** tests |
| 2 | `src/net/RateLimiter.luau`: key the entries on `key` only, dropping `playerId` | **AC-2** (`B at t + 0.5 ... -> REJECTED`) **and AC-6** (`size()` and `forget` no longer see players: after p1 uses two keys size is 2, and `forget(p1)` drops nothing keyed by key). `## Notes` predicts "AC-2 alone"; that holds only for the wrapper-level form of the bug (passing one id to `allow`), which is what the `sharedTimestamp` control measures at `1 of 28` | **2** tests (**1** if the bug is in the wrapper) |
| 3 | `src/net/RateLimiter.luau`: `>=` -> `>` in the interval comparison | **AC-1** alone, ONE line: `the same player at t + 1.5, exactly the interval -> REJECTED: reason = "rate"`; the first call and t + 1.4 stay right. Measured on the stub, `1 of 28` | **1** test |
| 4 | `src/net/Wrapper.luau`: construct the limiter INSIDE the returned closure (new per call) | AC-1, AC-2, AC-3, AC-5, detail, clock - the same six as `ignoresRateLimit`, since a fresh limiter never refuses | **6** tests |
| 5 | `src/net/Wrapper.luau`: move the rate check above the phase check | **AC-5** (`5 violation(s)`: out-of-phase call `rejected for "rate"`, clock read on two phase-rejected calls, timestamp advanced) and **`Contract: context.clock.now() is read zero times ...`** (`3 violation(s)`). Measured on the stub, `2 of 28` | **2** tests |
| 6 | `src/net/Wrapper.luau`: construct a limiter whether or not `definition.rateLimit` is set (e.g. default 1.5) | **AC-4** (`49 of 50`, `read 50 time(s)`), plus `phase_test.luau :: AC-6`, `wrapper_test.luau :: AC-1` and `wrapper_test.luau :: AC-5` - the repeated-call sites at a frozen clock | **4** tests |
| 7 | `src/net/Wrapper.luau`: read `context.clock.now()` once outside the returned closure and compare that | AC-1, AC-3, AC-5 and the clock Contract test | **4** tests |
| 8 | `src/net/RateLimiter.luau`: make `forget` a no-op | **AC-6** alone (`size() is 2 after forget(p1); expected 1`, `... returned false (p1 is new again after forget ...)`) | **1** test |
| 9 | `src/net/RateLimiter.luau`: make `size()` count `(player, key)` pairs | **AC-6** alone (`size() is 2 after p1 used a second remote ...; expected 1`) | **1** test |
| 10 | `src/net/Wrapper.luau`: prefix the rate detail with `"rate: "` | **`Contract: a rate rejection's detail ...`** alone | **1** test |

Single-assertion candidates: 3, 8, 9, 10. Run 3 first - it is the criterion's
own boundary and the narrowest prediction - and 1 second, as `## Notes` asks,
expecting **two** reds rather than one.

### `## Callers of changed signatures`, re-checked against the tree

Re-run on the working tree after the tests were written, 2026-09-22:
`grep -rn rateLimit src tests` and `grep -rn "\.guard(" src tests`.

- **The story's list holds**, with one omission that changes nothing:
  `tests/net/wrapper_test.luau:49` also calls the real `Wrapper.guard`
  directly (the `Contract: Wrapper exports guard ...` test), and the story's
  enumeration of call sites did not name it. It passes
  `NetContract.tokenRemote`, which declares no `rateLimit`, and calls the
  guard zero times - so it takes AC-4's path like the other eighteen and is
  untouched by this story. Recorded so the count is honest: **19** call sites
  of the real guard in the tree, not 18.
- `tests/helpers/NetContract.luau:768` is still the only `rateLimit`
  declaration outside this story, still never guarded and never called.
- Every one of the 10 `NetContract.luau` and 8 `PhaseContract.luau` guard
  sites still passes a definition without a `rateLimit`. The
  `limitsWithoutRateLimit` control shows what happens if GREEN gets AC-4
  wrong: **three** of those sites - `NetContract` AC-1 and AC-5 (repeated
  valid calls at `Clock.manual(0)`) and `PhaseContract` AC-6 (one guard, four
  calls, two in phase) - go red, exactly the shape the story named.
- New guard sites this story adds: `RateContract.luau` builds one guard per
  check through `net.Wrapper.guard`; `RateStubs.luau:235` is a stub guard
  over `PhaseStubs`, not a caller of the real one.
- No production source calls `Wrapper.guard` or `RateLimiter.new`; there is
  still no driver.

### Contract blocks amended in RED

Three, each marked **"Amended in RED"** in place under `## Contract`, with the
reason beside it; GREEN builds what the amended text says:

1. **"What the limiter stores, and what `size()` counts"** - two observable
   consequences of the pinned shape: after `forget`, the player is new again
   (allowed at the very instant they were just refused), and `forget` of an
   unseen player is a no-op that does not change `size()`. *Reason:* AC-6's
   "their entry is removed" means removal from the outside, and `size()`
   alone can be satisfied by a counter.
2. **"Where 1.5 comes from"**, vacuity assertion 1 - "exactly one row names
   it" became "exactly one table row has it as its **first cell**".
   *Reason:* the name appears in `mechanics.md`, `playtest.md` and could
   appear in another row's rationale; a substring match over the document
   would count those, and first-cell anchoring is what makes a row the row.
3. **"The controls this story needs"** - three rows corrected against
   measurements (`recordsOnReject` fails AC-1 too; `rejectsEverything` lives
   in the wrapper; `sharedTimestamp` is the wrapper-level form) and two
   controls added (`detailOmitsName`, `capturesClockAtGuard`). The reasons
   are in the amendment note under the table.

No acceptance criterion was touched. The Contract's frozen-guard citations
were re-read rather than trusted and **still hold** (this tree):
`Tuning.luau:14` says scope is §1 and §5 only; `TuningSpec.luau:419-438`
(`TuningSpec.unspecified`) fails any module key named nowhere in those two
sections; `tuning.md:88` is the one row with the constant in its first cell,
in §3, value `1.5`, label **derived**.

### Notes for the implementer

- **AC-6 needs the module to exist first.** Its first assertion is the
  module's absence, by name; once `src/net/RateLimiter.luau` returns a table
  with `new`, the rest of the check runs and every line names what it
  measured.
- **The registry is module-level and shared across test files.** Every
  declaration in the tests uses `NetContract.uniqueName(label)`, so no test
  relies on a reset and none is expected. The AC-3 load case declares one
  remote and guards it twice, once per player.
- **The limiter must be built per `guard()` call, in the closure's enclosing
  scope**, as the Contract says: mutation 4 above is the other placement and
  the tests fail it six ways.
- **`src/net/` may not require `src/server/`** (`architecture.md` section 1);
  `PlayerId` is re-declared in `RateLimiter.luau`, not imported from
  `Wrapper`, because `Wrapper` will require `RateLimiter`.
- **The typecheck gate runs over `src` only**, so the tests are not
  typechecked and the two source files are; `lint` and `format` run over
  `tests` too and are clean over the six new files.
- **Timings**, local only, no CI number: `lune run test` ran in 23.9 s wall
  at 355 cases standalone; the same command reported 53 s inside
  `gates.sh --fast`, alongside the harness gate's own runs. The runner has no
  per-test timeout, so there is nothing to budget. The 28 new cases are pure
  table work except the provenance guard, which reads one file - the AC-3
  load case's 320 calls are 320 `clock:set` calls, and the whole of
  `rate_test.luau` is instant.
- **Tests green on arrival, and what earns them:** AC-4 (the
  `limitsWithoutRateLimit` control fails it `4 of 28`, alone among this
  story's checks, and the real-wrapper probe in `## Regressions` reds it on
  all three of its lines); AC-1's first call (the `rejectsEverything` control
  fails it `15 of 28`); the 14 controls (green by design - each asserts a
  check *fails* against a wrong bundle, and the baseline test pins that the
  checks are satisfiable); the 6 provenance tests (the guard is earned by the
  `tuning.md` probe in `## Regressions`; the five controls are the negative
  controls themselves). None guards production behaviour that exists
  correctly today except AC-4 and the provenance guard, and both were probed.
- **Verifications I could not run:** none. Every control executed here. What
  RED cannot do is confirm the controls' numbers against the shipped module -
  the table above measures the stub bundles - and that is GREEN's job:
  `rate_test.luau` green is the confirmation, and the mutation table is how
  the orchestrator checks it discriminates.

## Regressions

### The provenance guard passes on arrival, so it was earned by mutation of the real document (RED)

`tuning.md` and the suite agree today (1.5), so
`rate_limit_provenance_test.luau`'s guard is green on its first run and would
stay green whether or not it compared anything. Per `rules.md` it is earned by
mutating the specific thing it pins - the value cell of the document's row -
through `scripts/mutate.sh`, which restores the file and verifies the restore.
One probe, one run, the suite's own output; `git status --porcelain --
docs/wiki/game/tuning.md` was empty after it.

**Probe A - the document retuned to 2** (the value moved, the row intact):

    $ bash scripts/mutate.sh docs/wiki/game/tuning.md \
        's/^| `signal_rate_limit_seconds` | 1\.5 |/| `signal_rate_limit_seconds` | 2 |/' \
        -- sh -c 'lune run test 2>&1 | grep -E -A3 "^  FAIL  tests/net/rate_limit_provenance|passed,"'
    === mutate: docs/wiki/game/tuning.md (1 line(s) changed by s/^| `signal_rate_limit_seconds` | 1\.5 |/| `signal_rate_limit_seconds` | 2 |/) ===
      88 - | `signal_rate_limit_seconds` | 1.5 | **derived** | two purposes at once: ...
      88 + | `signal_rate_limit_seconds` | 2 | **derived** | two purposes at once: ...
      FAIL  tests/net/rate_limit_provenance_test.luau :: AC-1 provenance: docs/wiki/game/tuning.md names signal_rate_limit_seconds in exactly one table row, its value cell is a number, and that number equals the 1.5 the rate suite declares
            C:\Users\ryanc\Projects\first-roblox\tests\net\rate_limit_provenance_test:81: the suite's signal_rate_limit_seconds = 1.5 does not match the document, which is the oracle (AC-1 calls it settled: read out, never tuned):
    docs/wiki/game/tuning.md:88 §3: `signal_rate_limit_seconds` is 2 in the document; the suite declares 1.5 - the document is the oracle and the suite's literal must follow it
    347 passed, 8 failed
    === mutate: command exited 0; restored (verified byte-for-byte against /c/Users/ryanc/Projects/first-roblox/.claude/state/mutations/docs_wiki_game_tuning.md.20260922T161305Z.3856915.bak) ===
      88: | `signal_rate_limit_seconds` | 1.5 | **derived** | two purposes at once: ...

`command exited 0` is `grep`'s exit, the last stage of the `sh -c` pipe; the
red is in the output. `8 failed` is the seven wrapper reds plus this guard;
the five in-file provenance controls are unaffected because they use in-memory
fixtures, and `rate_test.luau`'s seven are unchanged because the document is
not what the wrapper reads.

### AC-4 passes on arrival, so it was earned by mutation of the real wrapper (RED)

The current `Wrapper.luau` never reads the clock and accepts every seated,
valid, in-phase call, which is what AC-4 asks of a remote with no `rateLimit`.
The control suite earns it (`limitsWithoutRateLimit`), and so does one probe
of the real file: the wrapper made to read the clock and refuse every call
that reaches the handler. `git status --porcelain -- src` was empty after it.

**Probe B - the real wrapper reads the clock and refuses everything for rate:**

    $ bash scripts/mutate.sh src/net/Wrapper.luau \
        's/^\t\thandler(playerId, rawArgs)$/\t\tif context.clock.now() >= 0 then local probe: Rejection = { reason = "rate", detail = "probe" } return probe end/' \
        -- sh -c 'lune run test 2>&1 | grep -E -A5 "^  FAIL  tests/net/rate_test.luau :: AC-4|passed,"'
    === mutate: src/net/Wrapper.luau (1 line(s) changed by ...) ===
      162 - 		handler(playerId, rawArgs)
      162 + 		if context.clock.now() >= 0 then local probe: Rejection = { reason = "rate", detail = "probe" } return probe end
      FAIL  tests/net/rate_test.luau :: AC-4: a remote declared with no rateLimit accepts fifty calls at one instant, runs the handler fifty times, and never reads context.clock.now() - not during guard() and not during any call
            C:\Users\ryanc\Projects\first-roblox\tests\helpers\RateContract:485: AC-4: 3 violation(s) for a remote with no rateLimit:
      AC-4: 50 of 50 calls were not accepted; the first: AC-4: call 1 at the same instant -> REJECTED: reason = "rate", detail = "probe"
      AC-4: the handler ran 0 time(s) over 50 calls
      AC-4: context.clock.now() was read 50 time(s) across guard() and 50 calls; a remote with no rateLimit never reads the clock
    339 passed, 16 failed
    === mutate: command exited 0; restored (verified byte-for-byte against /c/Users/ryanc/Projects/first-roblox/.claude/state/mutations/src_net_Wrapper.luau.20260922T161400Z.3859920.bak) ===
      162: 		handler(playerId, rawArgs)

All three of AC-4's lines fire - the refusals, the handler count and the
structural read count - against the real file. The nine extra reds beyond the
arrival seven are the positive tests of NET-001 and NET-002 that a wrapper
refusing every call must also fail, and every one names the mutated file's
`"probe"` detail.

### `.claude/tests/project-counters.test.sh`: literals moved 72/72/15 -> 78/78/15, narrow unchanged at 15/15/7 (RED)

The `harness` gate (required) counts `.luau` files through the gates' own
evidence lines and pins the count as literals. This RED added six test files
under `tests/` and nothing under `src`, so `BASE_FORMAT`/`BASE_LINT` moved
72 -> 78 and nothing else did: `BASE_TYPECHECK` (15, `src`) and the three
narrow literals (`src` alone, `src/shared` alone) are untouched because no
`.luau` was added under `src`. The file's own header prescribes this in RED,
and NET-002's, NET-001's, TEL-001's and SEAT-002's REDs did the same. The
numbers were read from the harness gate's own failure lines in
`gates.sh --fast` - `actual count: 78` against `stylua over 78 files` and
`selene over 78 files`, `79` with the untracked probe present - not counted
by hand; the narrow assertions did not fail, which is the measurement that
they did not move. The eight assertion LABELS carrying the old numbers moved
with them, and the header's "LAST MEASURED" paragraph was added.

**Before**, from `.claude/state/gate-logs/harness.log` of the first
`gates.sh --fast`, with the six files present and the literals at 72/72/15:

        FAIL the working tree carries no stray .luau files, so the baselines mean what they say
             expected:
             actual:   ?? tests/helpers/RateContract.luau
        FAIL format reports 72 files on the unmodified tree
             expected count: 72
             actual count:   78
        FAIL lint reports 72 files on the unmodified tree
             expected count: 72
             actual count:   78
        FAIL AC-2: format counts the untracked file (72 -> 73)
             expected count: 73
             actual count:   79
        FAIL AC-4: lint counts the untracked file (72 -> 73)
             expected count: 73
             actual count:   79
        FAIL format does not count the ignored file (still 72)
             expected count: 72
             actual count:   78
        FAIL lint does not count the ignored file (still 72)
             expected count: 72
             actual count:   78
    project-counters: 33 passed, 7 failed

**After** the literals and labels moved:

    $ bash .claude/tests/project-counters.test.sh
        FAIL the working tree carries no stray .luau files, so the baselines mean what they say
             expected:
             actual:   ?? tests/helpers/RateContract.luau
                       ?? tests/helpers/RateLimitSpec.luau
                       ?? tests/helpers/RateStubs.luau
                       ?? tests/net/rate_controls_test.luau
                       ?? tests/net/rate_limit_provenance_test.luau
                       ?? tests/net/rate_test.luau
    project-counters: 39 passed, 1 failed

The one remaining failure is the stray-file precondition: the six test files
are untracked until the RED commit, and `git status --porcelain` lists them.
It clears on a clean tree; the orchestrator should see `40 passed, 0 failed`
once the commit lands, as NET-002's did.

## Gate probes

### The `unit` gate's floor, raised 327 -> 355 and watched to fail (GATES, Lead PO)

`project.conf` is the Lead PO's file per `rules.md`, so GREEN left the floor at
`327` while the real count was `355`; `## Notes` asks the orchestrator to raise
it, and this is that raise. The floor is part of what the `unit` gate *judges* —
it is what catches a suite that quietly went from 355 tests to 3 while still
exiting 0 and still matching the evidence regex — so moving it is changing a
gate, and a gate nobody has watched fail is not evidence of anything.

Probed by putting the floor one above the measured count through
`scripts/mutate.sh`, which restores the file and verifies the restore, and
running the gate it governs. A partial run, which `gates.sh` does not record:

    $ bash scripts/mutate.sh .claude/harness/project.conf \
        's/^floor    | unit      | 355$/floor    | unit      | 356/' \
        -- bash scripts/gates.sh --gate unit

    --- gate summary ---
    FAIL         unit (48s, did 355 units of work, below the floor of 356 in project.conf) -> .claude/state/gate-logs/unit.log

    changes: 2 changed source path(s), all exercised by a required gate

    (not recorded in the story: a partial run is not evidence of anything)

    1 required gate(s) failed.

    === mutate: command exited 1; restored (verified byte-for-byte against
        .../mutations/.claude_harness_project.conf.20260922T172241Z.4059690.bak) ===
      529: floor    | unit      | 355

So the floor is live in both directions: `355 passed, 0 failed` is **not** enough
to pass the gate when the floor claims 356, which is exactly the failure a
silently shrunken suite would produce. 355 is this story's own full run rather
than a guess, and `rules.md` forbids ever lowering it to make a gate pass.

### The `harness` gate's counter literals, moved in GREEN and watched to fail there

Not a second probe of the same thing: the `harness` gate's assertions are
counters, and they were **observed failing against this tree** before they were
moved, which is the same evidence read from the other side. Twelve of its forty
assertions were red at the moment `src/net/RateLimiter.luau` appeared, each
naming the number it measured (`expected count: 78` against `actual count: 79`,
`expected count: 15` against `actual count: 16`), and the narrow `src/shared`
assertion did **not** fail, which is the measurement that it did not move. The
full before-and-after is in `## Notes`, "The orchestrator's GREEN acceptance",
item 5, with the suite at `39 passed, 1 failed` after the move and `40 passed,
0 failed` once the source files were committed.

**No other gate was added or reconfigured.** `required_gates` stays `[]` (PO
decision 4): `unit` is required and `covers | unit | src/net/**` already brings
both changed source paths under it — this run reports `2 changed source path(s),
all exercised by a required gate`. The `tuning.md` §3 provenance reader and the
`RateLimiter` unit checks are ordinary tests inside the `unit` gate, not gates of
their own, and the ones that were green on arrival were earned by `mutate.sh`
probes recorded in `## Regressions`.

## Gate results

<!-- gates.sh: written by bash scripts/gates.sh; do not edit or paste by hand -->

    run:    2026-09-22T17:33:31Z
    commit: 1fcd1d9
    tree:   ceb26d17e61c282e4867833e36ef15e1950a822c
    result: pass (6 ran, 3 unconfigured, 0 known)

    PASS         format (1s, observed 79)
    PASS         lint (0s, observed 79, floor 1)
    PASS         typecheck (3s, observed 16)
    PASS         unit (27s, observed 355, floor 355)
    UNCONFIGURED coverage
    UNCONFIGURED integration
    PASS         build (1s, observed 47816)
    PASS         harness (23s, observed 40)
    UNCONFIGURED mutation

